import { Kind } from './event';
export declare function parseConnectionString(connectionString: string): {
    pubkey: string;
    relay: string;
    secret: string;
};
export declare function makeNwcRequestEvent({ pubkey, secret, invoice, }: {
    pubkey: string;
    secret: string;
    invoice: string;
}): Promise<import("./event.ts").VerifiedEvent<Kind>>;
