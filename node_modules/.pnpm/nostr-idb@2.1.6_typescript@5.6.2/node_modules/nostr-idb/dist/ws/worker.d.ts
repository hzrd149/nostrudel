import { AbstractWebSocket } from "./abstract-websocket.js";
export declare class WorkerWebSocket extends AbstractWebSocket {
    worker: Worker;
    constructor();
    close(): void;
}
export declare class SharedWorkerWebSocket extends AbstractWebSocket {
    worker: SharedWorker;
    constructor();
    close(): void;
}
