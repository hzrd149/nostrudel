import React, { ReactNode } from 'react';
import { MultiBackendOptions } from 'dnd-multi-backend';
export declare const PreviewPortalContext: React.Context<Element | null>;
export type DndProviderProps = {
    context?: any;
    options: MultiBackendOptions;
    children?: ReactNode;
    debugMode?: boolean;
    portal?: Element;
};
export declare const DndProvider: ({ portal, ...props }: DndProviderProps) => JSX.Element;
