import { TreeFile } from "./FileTree/TreeFile.js";
import { TreeFolder } from "./FileTree/TreeFolder.js";
export declare function getPathFromTag(tag: string[]): string[];
/** Creates a new TreeFile from a single "x" tag */
export declare function createTreeFileFromTag(tag: string[]): TreeFile;
/**
 * Creates a TreeFolder from a bunch of "x" tags
 * @example
 * const tree = createTreeFromTags(event.tags)
 */
export declare function createTreeFromTags(tags: string[][], quite?: boolean): TreeFolder;
export declare function createTagsForTree(root: TreeFolder, keepEmpty?: boolean): string[][];
/**
 * Takes an array of tags and updates all the "x" tags to reflect the current tree
 * @example
 * const tags = event.tags
 * const newTags = updateTreeInTags(tags, tree)
 * const newEvent = {
 *   kind: event.kind,
 *   content: event.content,
 *   created_at: Math.floor(Date.now()/1000),
 *   tags: newTags
 * }
 *
 * publish(await signer(newEvent))
 */
export declare function updateTreeInTags(tags: string[][], root: TreeFolder, keepEmpty?: boolean): string[][];
