export class Branch {
    get path() {
        if (this.parent)
            return [...this.parent.path, this.name];
        else
            return this.name ? [this.name] : [];
    }
    constructor(name) {
        this.name = name;
    }
}
