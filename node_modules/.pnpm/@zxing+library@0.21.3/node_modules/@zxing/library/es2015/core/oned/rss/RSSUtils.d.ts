/**
 * RSS util functions.
 */
export default class RSSUtils {
    private constructor();
    static getRSSvalue(widths: number[], maxWidth: number, noNarrow: boolean): number;
    private static combins;
}
