import { RefObject } from 'react';
import { DragLayerMonitor } from 'react-dnd';
export type Point = {
    x: number;
    y: number;
};
export declare const calculatePointerPosition: (monitor: DragLayerMonitor, childRef: RefObject<Element>) => Point | null;
