---
phase: 04-dead-code-and-import-hygiene-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/**/*.ts
  - src/**/*.tsx
  - src/services/wallets.ts
autonomous: true
requirements: [D-02, D-02a, D-03, D-04, D-04a]
must_haves:
  truths:
    - "The mechanically auto-fixable share of bucket C is applied in its own reviewable commit containing import-line changes only (D-04)"
    - "No narrative-comment finding is swept in — backlog 999.8 is left exactly as it was (D-03)"
    - "No vendored file under src/lib/ is modified, even though the fixer ignores the config exclude list"
    - "The unsafe aislop fix plan is never run (D-04a)"
    - "The bucket-C baseline is re-measured at execution time rather than trusted from the ROADMAP's stale 445 (D-02)"
  artifacts:
    - "One auto-fix commit touching ~96 files"
    - "One follow-up commit merging the duplicate import in src/services/wallets.ts by hand"
  key_links:
    - "aislop fix --safe output feeds every later wave; all manual plans run against the already-fixed tree"
---

<objective>
Apply the 132 auto-fixable bucket-C findings — 85 unused imports and 47 duplicate imports — as a
single reviewable commit, with every out-of-scope change the fixer makes reverted before staging.

Purpose: This is the ROADMAP's explicit ask, and it must land first so no later plan collides with
the fixer's edits. A reviewer should be able to skim this commit and see only import lines change.
Output: ~96 files with cleaner imports, backlog 999.8's comment findings untouched, and a measured
post-fix count that every later plan is verified against.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-RESEARCH.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md
@AGENTS.md
</context>

<measured_facts>
Measured at `a84d924eb` during planning. Re-measure at execution; a mismatch is a documentation gap
to correct in the summary, not a regression to investigate (04-CONTEXT.md § Specific Ideas).

**Bucket-C baseline: 417.** Per rule: `eslint/no-unused-vars` 208, `ai-slop/unused-import` 85,
`import/no-duplicates` 51, `ai-slop/duplicate-import` 47, `eslint/no-unreachable` 9,
`eslint/no-unused-expressions` 6, `ai-slop/unreachable-code` 1, `ai-slop/empty-function` 1,
`unicorn/no-useless-spread` 2, `typescript/no-unnecessary-parameter-property-assignment` 2, and one
each of `eslint/no-extra-boolean-cast`, `eslint/no-shadow-restricted-names`,
`eslint/no-useless-rename`, `unicorn/no-new-array`, `unicorn/no-useless-length-check`.

**Fixer scope:** 85 unused-import findings across 57 files, 47 duplicate-import findings across 44
files — 96 distinct files in the union.

**The fixer applies a third step this phase does not want:** narrative comments, currently 21
findings across exactly these 6 files — `src/components/webxdc/webxdc.tsx`,
`src/services/wallets.ts`, `src/components/webxdc/game-controls.tsx`,
`src/views/signin/connect/index.tsx`, `src/views/webxdc/components/webxdc-player.tsx`,
`src/services/pending-unlock.ts`. There is no flag to disable a step.

**Research measured the per-file diff shape of all six.** Five are comment-only with zero import
changes, so a wholesale revert is exact. Only `src/services/wallets.ts` is mixed: the fixer merges
two `applesauce-common/helpers` import statements into one while also deleting 6 banner comments.
This plan therefore reverts wallets.ts wholesale too and re-applies its import merge by hand
(04-RESEARCH.md Open Question 2's recommendation), which avoids hunk surgery entirely.

**The fixer does not honour the config exclude list.** In research it stripped 17 comment lines from
vendored `src/lib/qrcodegen.ts`. `git status --short src/lib/` must be empty after every fixer run.

**Expected post-fix counts** (research-measured): `ai-slop/unused-import` 0,
`ai-slop/duplicate-import` 0, `import/no-duplicates` 4, `eslint/no-unused-vars` 125, bucket-C total
155. The drop is larger than the nominal 132 because the rule sets overlap — D-02a says explicitly
that a larger-than-expected drop is not an error.

**Four `import/no-duplicates` findings survive by design** — in `embedded-zap-receipt.tsx`,
`app-tabs-layout.tsx`, `timeline/highlight.tsx` and `badges/badge-details.tsx`, where the duplicate
straddles the blank line separating external from internal imports and the fixer will not merge
across that boundary. They are 04-10's work, not this plan's.

**About 20 `eslint/no-unused-vars` unused-import findings also survive** the fixer (only 64 of the
103 share an exact line with an `ai-slop/unused-import` finding). Those are also 04-10's work.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Run the safe auto-fix and revert everything outside bucket C</name>
  <files>src/**/*.ts, src/**/*.tsx</files>
  <read_first>
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-RESEARCH.md § Auto-Fix Mechanics (the per-file diff table)
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md (the quick-run command and both pitfalls)
    - AGENTS.md § Linting
  </read_first>
  <action>
    Record the bucket-C baseline first using the quick-run command from 04-VALIDATION.md, plus a
    per-rule breakdown, and keep both numbers for the summary.

    Run the safe fixer over the repo: `pnpm exec aislop fix --safe .`. Never run `aislop fix`
    without `--safe` (D-04a) — the unsafe plan adds dead-code, unused-declaration and general lint
    steps totalling roughly 950 findings far outside bucket C, and would delete code without
    per-site judgment. If you accidentally run it, discard the working tree and start over.

    Then revert every change outside this phase's scope, before staging anything:

    Check `git status --short src/lib/` and, if it reports anything, `git checkout -- src/lib/` to
    restore all vendored code wholesale. The config exclude list does not protect these files from
    the fixer.

    Restore all six narrative-comment-contaminated files wholesale with `git checkout --`:
    `src/components/webxdc/webxdc.tsx`, `src/components/webxdc/game-controls.tsx`,
    `src/views/signin/connect/index.tsx`, `src/views/webxdc/components/webxdc-player.tsx`,
    `src/services/pending-unlock.ts`, and `src/services/wallets.ts`. Five of these have no import
    change to preserve; wallets.ts does, and Task 2 re-applies it by hand from a clean base.

    Now review the remaining diff. Every surviving hunk must be an import-line change. If any file
    shows a comment-only or otherwise non-import hunk, revert that file too and record it in the
    summary as a seventh contaminated file — research already found one file D-03 did not name, so
    treat the named list as a floor rather than a closed set.

    Commit this alone, touching no other change (D-04). This is the reviewable mechanical commit the
    ROADMAP asks for.
  </action>
  <verify>
    <automated>git status --short src/lib/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `git status --short src/lib/ | wc -l` outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/narrative-comment")]|length'` outputs `21` — backlog 999.8 is untouched.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/unused-import")]|length'` outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/duplicate-import")]|length'` outputs `0`.
    - `git show --stat HEAD` lists no path under `src/lib/`.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>The fixer's import work is committed alone, vendored code is untouched, and the narrative-comment count is unchanged at 21.</done>
</task>

<task type="auto">
  <name>Task 2: Re-apply the wallets.ts duplicate-import merge by hand</name>
  <files>src/services/wallets.ts</files>
  <read_first>
    - src/services/wallets.ts (the full import block at the top of the file)
    - .planning/codebase/CONVENTIONS.md § Import Organization
  </read_first>
  <action>
    Task 1 reverted this file wholesale to protect its 6 banner comments, which also discarded the
    one legitimate import fix the fixer had made. Re-apply only that fix, by hand.

    The file imports from `applesauce-common/helpers` twice: once for the value imports
    `parseBolt11` and `parseLNURLOrAddress`, and once as a type-only import of
    `EncryptedContentCache`. Merge them into the single statement at the position of the first,
    carrying the type import across with an inline `type` modifier on that specifier so the
    type-only semantics are preserved under `isolatedModules`. Delete the now-redundant second
    statement.

    Change nothing else in this file. All 6 banner comments stay, and the two dead constants stay
    for now — 04-03 deletes those in the next wave.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/services/wallets.ts")|select(.rule=="import/no-duplicates" or .rule=="ai-slop/duplicate-import" or .rule=="ai-slop/unused-import")]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/services/wallets.ts")|select(.rule=="ai-slop/narrative-comment")]|length'` outputs `6`.
    - `git diff HEAD~1 -- src/services/wallets.ts` shows changes confined to the import block.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>wallets.ts has one merged import, its six banner comments, and no duplicate-import finding.</done>
</task>

<task type="auto">
  <name>Task 3: Record the wave-1 per-rule before/after measurement</name>
  <files>none — measurement only</files>
  <read_first>
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md (the full-run command)
  </read_first>
  <action>
    Run the full-run per-file command from 04-VALIDATION.md and the per-rule quick counts, and
    record in the summary a before/after table covering all fifteen bucket-C rules, starting from
    the 417 baseline captured in Task 1.

    State the measured bucket-C total explicitly. Research measured 155; if the real number differs,
    record the actual value and reconcile it per rule rather than treating the difference as a
    defect — D-02a warns that the overlap between `ai-slop/unused-import` and
    `eslint/no-unused-vars` makes the drop larger than the nominal 132 fixes.

    Also record, for 04-10's benefit, the list of files still reporting `import/no-duplicates` and
    the list still reporting an `eslint/no-unused-vars` finding whose message says a symbol is
    imported but never used. These are the stragglers the fixer cannot reach.

    Confirm the Phase 3 bar still holds: `ai-slop/swallowed-exception` must still be 0 at every
    severity. Make no source edits in this task.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/swallowed-exception")]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0` — Phase 3's bar is intact.
    - The summary contains a fifteen-row per-rule before/after table whose "before" column sums to the recorded baseline.
    - The summary states the measured post-fix bucket-C total and, if it is not 155, explains the difference per rule.
    - The summary lists the surviving `import/no-duplicates` files and the surviving unused-import findings for 04-10.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>Wave 1's measurement is recorded and every later plan has a verified starting point.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated fixer → working tree | The tool rewrites ~96 files unattended, and its file selection ignores the config exclude list |
| phase scope → backlog scope | The fixer's third step reaches into backlog 999.8's comment findings |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-03 | Tampering | `aislop fix --safe` editing vendored `src/lib/**` | high | mitigate | `git status --short src/lib/` is checked after the fixer run and is an acceptance criterion; any change is reverted wholesale before staging. Vendored code is third-party and must not be silently modified. |
| T-04-04 | Tampering | fixer sweeping backlog-999.8 narrative comments into a hygiene commit | medium | mitigate | All six named files reverted wholesale; the narrative-comment count is asserted to still read 21. The named list is treated as a floor — any other non-import hunk is reverted and reported. |
| T-04-12 | Tampering | running the unsafe fix plan, deleting ~950 findings of code with no review | high | mitigate | D-04a is stated in the action with the recovery step (discard the tree). Only `--safe` is ever invoked. |
| T-04-13 | Repudiation | mechanical churn burying a judgment call in one commit | medium | mitigate | D-04's own-commit rule; the manual wallets.ts merge is a separate commit so the mechanical commit stays skimmable. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed; `package.json` and `pnpm-lock.yaml` untouched, so no dependency audit is triggered. |
</threat_model>

## Artifacts this phase produces

**Removed**
- 85 unused import specifiers across 57 files
- 47 duplicate import statements across 44 files, merged in place at the position of the first

**Added**
- Nothing. This plan adds no code and no ignore directives.

**Deliberately NOT changed**
- All 21 narrative comments in the six named files (backlog 999.8)
- Every vendored path under `src/lib/`
- The two dead constants in `src/services/wallets.ts` (04-03 deletes them)
- The 4 residual `import/no-duplicates` sites and ~20 residual unused imports (04-10)

<verification>
- `git status --short src/lib/` is empty.
- `ai-slop/unused-import` and `ai-slop/duplicate-import` both read 0.
- `ai-slop/narrative-comment` still reads 21.
- `ai-slop/swallowed-exception` still reads 0.
- A fifteen-row per-rule before/after table is recorded in the summary.
</verification>

<success_criteria>
The auto-fixable share of bucket C is committed as one skimmable import-only commit, nothing outside
bucket C changed, and the post-fix baseline every later plan measures against is recorded.
</success_criteria>

<output>
Create `.planning/phases/04-dead-code-and-import-hygiene-sweep/04-01-SUMMARY.md` when done.
</output>
</content>
