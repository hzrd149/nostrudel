---
phase: 04-dead-code-and-import-hygiene-sweep
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/classes/encrypted-storage.tsx
  - src/helpers/lightning.ts
  - src/helpers/lnurl.ts
  - src/helpers/nostr/event.ts
  - src/helpers/nostr/torrents.ts
  - src/helpers/nostr/zaps.ts
  - src/services/event-cache/native-sqlite.ts
  - src/services/notifications/zaps.ts
  - src/services/relay-info.ts
  - src/components/app-handler-modal/index.tsx
  - src/components/content/links/nostr.tsx
  - src/providers/route/delete-event-provider.tsx
  - src/views/channels/components/channel-message-block.tsx
  - src/views/groups/components/group-message-group.tsx
  - src/views/messages/components/direct-message-group.tsx
  - src/views/settings/accounts/components/password-signer-backup.tsx
  - src/views/settings/media-servers/index.tsx
  - src/views/settings/privacy/index.tsx
  - src/views/settings/profile/components/profile-edit-form.tsx
  - src/views/streams/stream/components/stream-sats-per-minute.tsx
  - src/views/wallet/components/receive-token-modal.tsx
autonomous: true
requirements: [D-05]
must_haves:
  truths:
    - "All 24 catch clauses that discard their caught binding use the bare form documented in AGENTS.md § Swallowed Exceptions (D-05)"
    - "No catch body is altered, so Phase 3's error-severity swallowed-exception bar stays at 0"
    - "No catch block is left literally empty"
  artifacts:
    - "21 modified source files"
  key_links:
    - "Each bare catch keeps the control flow or logging call that already satisfies ai-slop/swallowed-exception"
---

<objective>
Drop the unused caught binding from all 24 catch clauses that declare one and never read it,
converting each to the bare catch form Phase 3 established.

Purpose: These are the sites Phase 3 never reached because it only rewrote lines it was already
touching. The remedy is the narrowest possible edit — remove the binding, change nothing else.
Output: 24 catch clauses converted across 21 files, with the swallowed-exception count still 0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md
@AGENTS.md
</context>

<measured_facts>
The 24 sites, measured at `a84d924eb`. Line numbers shift after 04-01's import deletions — locate
each by finding the catch clause that declares a binding the body never reads.

| File | Lines | Bindings |
|---|---|---|
| `src/classes/encrypted-storage.tsx` | 126, 134 | `e`, `e` |
| `src/helpers/lightning.ts` | 33 | `error` |
| `src/helpers/lnurl.ts` | 6 | `e` |
| `src/helpers/nostr/event.ts` | 35 | `error` |
| `src/helpers/nostr/torrents.ts` | 66 | `e` |
| `src/helpers/nostr/zaps.ts` | 29 | `err` |
| `src/services/event-cache/native-sqlite.ts` | 197 | `err` |
| `src/services/notifications/zaps.ts` | 81 | `error` |
| `src/services/relay-info.ts` | 19 | `error` |
| `src/components/app-handler-modal/index.tsx` | 150 | `error` |
| `src/components/content/links/nostr.tsx` | 18 | `error` |
| `src/providers/route/delete-event-provider.tsx` | 87 | `e` |
| `src/views/channels/components/channel-message-block.tsx` | 25 | `error` |
| `src/views/groups/components/group-message-group.tsx` | 41 | `error` |
| `src/views/messages/components/direct-message-group.tsx` | 45 | `error` |
| `src/views/settings/accounts/components/password-signer-backup.tsx` | 40 | `error` |
| `src/views/settings/media-servers/index.tsx` | 125 | `error` |
| `src/views/settings/privacy/index.tsx` | 30, 43 | `e`, `e` |
| `src/views/settings/profile/components/profile-edit-form.tsx` | 57, 82 | `error`, `error` |
| `src/views/streams/stream/components/stream-sats-per-minute.tsx` | 55 | `e` |
| `src/views/wallet/components/receive-token-modal.tsx` | 53 | `error` |

**This is a narrower job than Phase 3's.** `ai-slop/swallowed-exception` currently reads 0 at every
severity repo-wide, which means every one of these catch bodies already contains the control flow or
logging call the rule requires. The only defect is the unread binding. Removing just the binding
therefore cannot regress Phase 3's bar — but changing a body could, so do not.

**Two files carry inherited error-severity findings.**
`src/components/app-handler-modal/index.tsx` has 3 pre-existing `react-hooks/rules-of-hooks` errors
and `src/views/settings/accounts/components/password-signer-backup.tsx` has 2. Touching these files
makes `pnpm lint:ci` report those inherited errors on this branch. That is expected and must not be
fixed here (D-15, backlog 999.2); 04-11 enumerates them and proves they predate the phase.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Bare catch in helpers, classes and services (10 sites, 9 files)</name>
  <files>src/classes/encrypted-storage.tsx, src/helpers/lightning.ts, src/helpers/lnurl.ts, src/helpers/nostr/event.ts, src/helpers/nostr/torrents.ts, src/helpers/nostr/zaps.ts, src/services/event-cache/native-sqlite.ts, src/services/notifications/zaps.ts, src/services/relay-info.ts</files>
  <read_first>
    - Each file being modified, reading the full catch block at the listed line
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md § D-05 (the target shape, taken from src/helpers/nip19.ts)
    - AGENTS.md § Swallowed Exceptions
  </read_first>
  <action>
    For each listed site, remove the parenthesised caught binding from the catch clause so it becomes
    a bare catch, and leave the block body byte-for-byte unchanged. This is the shape already used
    in `src/helpers/nip19.ts`, `src/helpers/parse.ts` and `src/helpers/nostr/goal.ts`.

    Before editing each one, read the body and confirm the binding really is unread — if the body
    references it in a way the linter missed, do not convert that site and record it in the summary.

    Do not add, remove or reword any comment, log call, return, continue or any other statement in
    these bodies. The bodies already satisfy `ai-slop/swallowed-exception`, which is error-severity
    and gates CI; editing them is the only way this task could regress Phase 3's result.

    The one exception: if you find a body that is literally empty or contains only a comment, the
    bare catch alone would leave it firing `eslint/no-empty` and probably
    `ai-slop/swallowed-exception`. In that case apply AGENTS.md's deliberate parse-guard shape —
    keep a reason comment and add the explicit exit that reproduces the value the code already fell
    through to — and call the site out in the summary as having needed more than a binding removal.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-unused-vars" and (.message|test("caught but never used")))|select(["src/classes/encrypted-storage.tsx","src/helpers/lightning.ts","src/helpers/lnurl.ts","src/helpers/nostr/event.ts","src/helpers/nostr/torrents.ts","src/helpers/nostr/zaps.ts","src/services/event-cache/native-sqlite.ts","src/services/notifications/zaps.ts","src/services/relay-info.ts"]|index(.filePath))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/swallowed-exception")]|length'` outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-empty")]|length'` outputs the same value recorded before this task began.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>Ten catch clauses in helpers, classes and services are bare, with unchanged bodies and a still-zero swallowed-exception count.</done>
</task>

<task type="auto">
  <name>Task 2: Bare catch in components and providers (3 sites, 3 files)</name>
  <files>src/components/app-handler-modal/index.tsx, src/components/content/links/nostr.tsx, src/providers/route/delete-event-provider.tsx</files>
  <read_first>
    - Each file being modified, reading the full catch block at the listed line
    - AGENTS.md § Swallowed Exceptions
  </read_first>
  <action>
    Apply the same binding-removal to the three listed sites, leaving every catch body unchanged.

    `src/components/app-handler-modal/index.tsx` carries 3 pre-existing
    `react-hooks/rules-of-hooks` errors near the top of the component. Do not fix them, do not
    reorder the hooks, and do not add an ignore for them — they are backlog 999.2 and explicitly out
    of scope by D-15 and by the project's no-sweep override in `.claude/CLAUDE.md`. Touching this
    file for the catch clause is expected to make `pnpm lint:ci` surface those inherited errors on
    this branch; record that in the summary rather than acting on it.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-unused-vars" and (.message|test("caught but never used")))|select(["src/components/app-handler-modal/index.tsx","src/components/content/links/nostr.tsx","src/providers/route/delete-event-provider.tsx"]|index(.filePath))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/components/app-handler-modal/index.tsx")|select(.rule=="react-hooks/rules-of-hooks")]|length'` still outputs `3` — the inherited errors were neither fixed nor suppressed.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>Three more catch clauses are bare and the inherited hook-order errors are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Bare catch across views (11 sites, 9 files)</name>
  <files>src/views/channels/components/channel-message-block.tsx, src/views/groups/components/group-message-group.tsx, src/views/messages/components/direct-message-group.tsx, src/views/settings/accounts/components/password-signer-backup.tsx, src/views/settings/media-servers/index.tsx, src/views/settings/privacy/index.tsx, src/views/settings/profile/components/profile-edit-form.tsx, src/views/streams/stream/components/stream-sats-per-minute.tsx, src/views/wallet/components/receive-token-modal.tsx</files>
  <read_first>
    - Each file being modified, reading the full catch block at each listed line
    - AGENTS.md § Swallowed Exceptions
  </read_first>
  <action>
    Apply the same binding-removal to the eleven remaining sites. Three files have two sites each —
    `settings/privacy/index.tsx` and `settings/profile/components/profile-edit-form.tsx` — so check
    both catch clauses in those files rather than stopping at the first.

    `password-signer-backup.tsx` carries 2 pre-existing `react-hooks/rules-of-hooks` errors. As in
    Task 2, do not fix or suppress them (D-15).

    `stream-sats-per-minute.tsx` also has an unused parameter that 04-05 handles in a later wave.
    Change only the catch clause here; leave the function signature alone.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-unused-vars" and (.message|test("caught but never used")))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0` — no unused catch binding remains anywhere in the repo, closing D-05 in full.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/swallowed-exception")]|length'` outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/views/streams/stream/components/stream-sats-per-minute.tsx")|select(.rule=="eslint/no-unused-vars" and (.message|test("^Parameter")))]|length'` still outputs `1` — 04-05's site was left for its owner.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>All 24 catch bindings are gone repo-wide and D-05 is closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caught error → handler body | The body is what satisfies the error-severity swallowed-exception rule; the binding is not |
| this phase → backlog 999.2 | Two touched files carry inherited hook-order errors that gate CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Repudiation | catch bodies losing their log or control-flow statement | high | mitigate | The action forbids editing bodies at all; every task asserts `ai-slop/swallowed-exception` still reads 0, which is Phase 3's gating bar. |
| T-04-06 | Repudiation | a body that is empty or comment-only becoming a silent failure path | medium | mitigate | The executor reads each body first; the empty/comment-only case gets AGENTS.md's parse-guard shape instead of a bare binding removal, and `eslint/no-empty` is asserted not to rise. |
| T-04-09 | Repudiation | inherited hook-order errors in two touched files | medium | accept | Backlog 999.2 / D-15. Asserted to remain exactly 3 in app-handler-modal so they are neither fixed nor suppressed; 04-11 proves they predate the phase. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed. |
</threat_model>

## Artifacts this phase produces

**Removed**
- The caught binding from 24 catch clauses across 21 files

**Added**
- Nothing, unless a body is found empty or comment-only, in which case AGENTS.md's parse-guard
  reason comment plus explicit exit is added at that site and reported

**Deliberately NOT changed**
- Every catch body's existing statements and comments
- The inherited hook-order errors in `app-handler-modal/index.tsx` and `password-signer-backup.tsx`
- The unused parameter in `stream-sats-per-minute.tsx` (04-05 owns it)

<verification>
- Catch-binding findings repo-wide: 24 to 0.
- `ai-slop/swallowed-exception` stays 0 at every severity.
- `eslint/no-empty` does not rise.
- `pnpm build` passes after every task.
</verification>

<success_criteria>
Every catch clause that discarded its binding now uses the bare form, no handler body changed, and
Phase 3's error-severity bar is still intact.
</success_criteria>

<output>
Create `.planning/phases/04-dead-code-and-import-hygiene-sweep/04-02-SUMMARY.md` when done.
</output>
</content>
