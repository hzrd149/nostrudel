---
phase: 04-dead-code-and-import-hygiene-sweep
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/components/note/note-menu.tsx
  - src/components/pow/miner.ts
  - src/components/timeline/highlight.tsx
  - src/services/outbox-subscriptions.ts
  - src/views/badges/badge-details.tsx
  - src/views/lists/components/fallback-list-card.tsx
  - src/views/napplets/napplet.tsx
  - src/views/relays/map/components/relay-details.tsx
  - src/views/relays/relay/tabs/about.tsx
  - src/views/search/components/search-results.tsx
  - src/views/settings/mailboxes/index.tsx
  - src/views/tools/event-console/user-autocomplete.ts
autonomous: true
requirements: [D-07]
must_haves:
  truths:
    - "The 13 plain dead local variables are deleted; git history is their record (D-07)"
    - "No deletion removes a call whose effect the surrounding code still depends on"
    - "Imports orphaned by these deletions are removed in the same edit"
  artifacts:
    - "12 modified source files"
  key_links:
    - "pnpm build is the backstop proving no remaining reference to any deleted binding"
---

<objective>
Delete the thirteen plain dead local variables — the straightforward half of D-07, separated from
04-03's judgment sites so this plan can move quickly without diluting scrutiny there.

Purpose: These bindings are declared and never read. Each is a one-line deletion, but two of them
are reassigned later in their function and two more may hold the result of a call worth keeping.
Output: 13 declarations removed across 12 files, with a passing typecheck after each task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md
@AGENTS.md
</context>

<measured_facts>
The 13 sites, measured at `a84d924eb`. Line numbers shift after 04-01 — locate each by binding name.

| File | Line | Binding | Note |
|---|---|---|---|
| `src/components/note/note-menu.tsx` | 21 | `address` | |
| `src/components/pow/miner.ts` | 10 | `bestHash` | message says *assigned a value* but never used — check for later reassignment |
| `src/components/timeline/highlight.tsx` | 113 | `highlightText` | two sites in this file |
| `src/components/timeline/highlight.tsx` | 114 | `context` | |
| `src/services/outbox-subscriptions.ts` | 37 | `authors` | |
| `src/views/badges/badge-details.tsx` | 98 | `navigate` | likely a `useNavigate()` result |
| `src/views/lists/components/fallback-list-card.tsx` | 87 | `isSpecialList` | file carries 1 inherited hook-order error |
| `src/views/napplets/napplet.tsx` | 98 | `intent` | |
| `src/views/relays/map/components/relay-details.tsx` | 37 | `url` | |
| `src/views/relays/relay/tabs/about.tsx` | 49 | `loading` | may be one element of a destructuring |
| `src/views/search/components/search-results.tsx` | 27 | `loadingProfiles` | may be one element of a destructuring |
| `src/views/settings/mailboxes/index.tsx` | 57 | `info` | |
| `src/views/tools/event-console/user-autocomplete.ts` | 7 | `lookupPromise` | message says *assigned a value* but never used — check for later reassignment |

**Two distinct message shapes appear here and they mean different things.** "declared but never
used" is a plain dead binding. "assigned a value but never used" — `bestHash` and `lookupPromise` —
means the binding is written somewhere and never read, so there may be later assignment statements
that also have to go. Read the whole enclosing function for those two.

**`timeline/highlight.tsx` and `badges/badge-details.tsx` are also 04-10 targets** for a residual
duplicate import, and `fallback-list-card.tsx` carries one pre-existing
`react-hooks/rules-of-hooks` error. 04-10 runs in a later wave; the hook-order error is backlog
999.2 and must not be fixed here (D-15).
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Delete the dead locals in components and services (5 sites, 4 files)</name>
  <files>src/components/note/note-menu.tsx, src/components/pow/miner.ts, src/components/timeline/highlight.tsx, src/services/outbox-subscriptions.ts</files>
  <read_first>
    - Each file being modified, reading the whole function that encloses the binding
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md § D-07 (plain deletion shape)
  </read_first>
  <action>
    Delete the `address` binding in `note-menu.tsx`, the `highlightText` and `context` bindings in
    `timeline/highlight.tsx`, and the `authors` binding in `services/outbox-subscriptions.ts`.

    `miner.ts`'s `bestHash` needs more care: its message says it is assigned but never read, so read
    the whole enclosing function and remove every assignment to it as well as the declaration. If
    removing those assignments would change what a surrounding expression computes, stop and record
    the site instead of forcing it.

    For each site, decide what the right-hand side is before deleting. If it is a plain expression
    or a pure getter, delete the whole statement. If it is a call whose effect the surrounding code
    still depends on, keep the call and prefix the binding with an underscore instead, and say so in
    the summary. If the binding is one element of a destructuring whose other elements are live,
    remove only that element.

    Remove any import your deletion orphans in the same edit. Do not remove imports that were
    already unused before your edit — 04-10 owns those.

    In `timeline/highlight.tsx`, change only these two bindings; the unused parameter in that file
    belongs to 04-05 and its duplicate import belongs to 04-10.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-unused-vars" and (.message|test("^Variable")))|select(["src/components/note/note-menu.tsx","src/components/pow/miner.ts","src/components/timeline/highlight.tsx","src/services/outbox-subscriptions.ts"]|index(.filePath))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/components/timeline/highlight.tsx")|select(.rule=="eslint/no-unused-vars" and (.message|test("^Parameter")))]|length'` still outputs `1` — 04-05's site was left alone.
    - `pnpm build` exits 0.
    - The summary records, for any site kept as an underscore-prefixed binding, why the call had to stay.
  </acceptance_criteria>
  <done>Five dead locals are gone from components and services with no orphaned imports and a passing typecheck.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the dead locals across views (8 sites, 8 files)</name>
  <files>src/views/badges/badge-details.tsx, src/views/lists/components/fallback-list-card.tsx, src/views/napplets/napplet.tsx, src/views/relays/map/components/relay-details.tsx, src/views/relays/relay/tabs/about.tsx, src/views/search/components/search-results.tsx, src/views/settings/mailboxes/index.tsx, src/views/tools/event-console/user-autocomplete.ts</files>
  <read_first>
    - Each file being modified, reading the whole function that encloses the binding
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md § D-07
  </read_first>
  <action>
    Delete `navigate`, `isSpecialList`, `intent`, `url`, `loading`, `loadingProfiles` and `info` at
    the listed sites, applying the same right-hand-side judgment as Task 1.

    Pay particular attention to three of these. `navigate` is almost certainly a `useNavigate()`
    result — a pure hook returning a function, so deleting the whole statement is safe, and the
    `useNavigate` import will usually be orphaned by it. `loading` and `loadingProfiles` read like
    single elements of a destructured hook result whose siblings are live — in that case remove only
    the dead element, never the whole statement.

    `user-autocomplete.ts`'s `lookupPromise` is the second "assigned a value but never used" case:
    read the whole module, remove the declaration and every assignment to it, and confirm nothing
    depends on the promise being created for its side effect. If it is created to start work whose
    completion matters elsewhere, keep the call and underscore-prefix the binding instead.

    Remove imports your deletions orphan. Do not fix `fallback-list-card.tsx`'s pre-existing
    hook-order error (D-15, backlog 999.2). Leave `badge-details.tsx`'s duplicate import for 04-10.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-unused-vars" and (.message|test("^Variable")))|select(["src/views/badges/badge-details.tsx","src/views/lists/components/fallback-list-card.tsx","src/views/napplets/napplet.tsx","src/views/relays/map/components/relay-details.tsx","src/views/relays/relay/tabs/about.tsx","src/views/search/components/search-results.tsx","src/views/settings/mailboxes/index.tsx","src/views/tools/event-console/user-autocomplete.ts"]|index(.filePath))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/views/lists/components/fallback-list-card.tsx")|select(.rule=="react-hooks/rules-of-hooks")]|length'` still outputs `1`.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.filePath=="src/views/badges/badge-details.tsx")|select(.rule=="import/no-duplicates")]|length'` still outputs a non-zero value — 04-10's straggler is untouched.
    - `pnpm build` exits 0.
  </acceptance_criteria>
  <done>All thirteen plain dead locals are gone, with 04-05's and 04-10's sites in these files left for their owners.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unused binding → statement | The rule flags the binding; the right-hand side may still carry an effect the code needs |
| this phase → backlog 999.2 | One touched file carries an inherited hook-order error that gates CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-01 | Tampering | deleting a statement whose right-hand side has an effect | high | mitigate | The action requires classifying the right-hand side before deleting, with an explicit underscore-prefix fallback when a call must stay; `pnpm build` catches every reference `tsc` can see. |
| T-04-14 | Tampering | removing only the declaration of a binding that is assigned elsewhere | medium | mitigate | The two "assigned but never used" sites are called out by name and require reading the whole enclosing function so later assignments go too. |
| T-04-15 | Tampering | deleting a whole destructuring whose siblings are live | medium | mitigate | Named for `loading` and `loadingProfiles`; only the dead element may be removed. `pnpm build` fails if a live sibling is dropped. |
| T-04-09 | Repudiation | inherited hook-order error in `fallback-list-card.tsx` | medium | accept | Backlog 999.2 / D-15; asserted to remain at 1 so it is neither fixed nor suppressed. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed. |
</threat_model>

## Artifacts this phase produces

**Removed**
- 13 dead local bindings: `address`, `bestHash`, `highlightText`, `context`, `authors`, `navigate`,
  `isSpecialList`, `intent`, `url`, `loading`, `loadingProfiles`, `info`, `lookupPromise`
- Any later assignments to `bestHash` and `lookupPromise`
- Any import orphaned by the deletions above

**Added**
- Nothing, unless a site must keep its call, in which case the binding is underscore-prefixed and
  the reason recorded

**Deliberately NOT changed**
- The unused parameter in `timeline/highlight.tsx` (04-05 owns it)
- The residual duplicate imports in `timeline/highlight.tsx` and `badge-details.tsx` (04-10)
- The inherited hook-order error in `fallback-list-card.tsx` (backlog 999.2)

<verification>
- Dead-variable findings in this plan's 12 files: 13 to 0.
- The neighbouring findings owned by 04-05 and 04-10 are still present and untouched.
- `pnpm build` passes after each task.
</verification>

<success_criteria>
Thirteen dead locals are gone, no live call or destructured sibling was removed with them, and every
deviation from a plain deletion is recorded with its reason.
</success_criteria>

<output>
Create `.planning/phases/04-dead-code-and-import-hygiene-sweep/04-04-SUMMARY.md` when done.
</output>
</content>
