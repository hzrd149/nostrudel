---
phase: 04-dead-code-and-import-hygiene-sweep
plan: 11
type: execute
wave: 5
depends_on: [04-10]
files_modified:
  - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md
autonomous: true
requirements: [D-01, D-14, D-15]
must_haves:
  truths:
    - "A per-rule before/after table covers all fifteen bucket-C rules, starting from the measured 417 baseline (D-14)"
    - "Every bucket-C finding still reported is a row in the Documented Ignores Ledger with a reason meeting D-08's bar (D-01)"
    - "Every error-severity finding pnpm lint:ci reports is proven to predate this phase (D-15)"
  artifacts:
    - "A completed Documented Ignores Ledger and signed-off 04-VALIDATION.md"
    - "A 04-11-SUMMARY.md carrying the phase's before/after report"
  key_links:
    - "The ledger is the phase gate — a count of zero achieved by deleting load-bearing code is a failure, not a pass"
---

<objective>
Close the phase: produce the per-rule before/after report, prove every surviving bucket-C finding is
a documented exception rather than an oversight, and record the disposition of the error-severity
findings this phase inherits but must not fix.

Purpose: D-01's completion test is not a number. It is that nothing is left un-triaged. This plan is
where that claim is actually checked, and where the one contradiction planning found in the
validation contract gets resolved on the evidence.
Output: a completed ledger, a signed-off validation document, and a summary that a reviewer can
audit without re-running anything.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md
@AGENTS.md
</context>

<measured_facts>
**The 417 baseline and its fifteen-rule split** are recorded in 04-01's summary, measured at
`a84d924eb`. Per rule: `eslint/no-unused-vars` 208, `ai-slop/unused-import` 85,
`import/no-duplicates` 51, `ai-slop/duplicate-import` 47, `eslint/no-unreachable` 9,
`eslint/no-unused-expressions` 6, `ai-slop/unreachable-code` 1, `ai-slop/empty-function` 1,
`unicorn/no-useless-spread` 2, `typescript/no-unnecessary-parameter-property-assignment` 2, and one
each of `eslint/no-extra-boolean-cast`, `eslint/no-shadow-restricted-names`,
`eslint/no-useless-rename`, `unicorn/no-new-array`, `unicorn/no-useless-length-check`.

**Expected end state.** Every rule should reach 0 except the two covered by the sqlite ignore —
`eslint/no-unreachable` and `ai-slop/unreachable-code` — which should also report 0 because the
directive suppresses them. The three `eslint/no-unused-expressions` sites behind ignores likewise
report 0. So the bucket-C count is expected to land at or near 0, but **a zero reached by deleting
load-bearing code is a failure**, which is why the ledger, not the count, is the gate.

**The four pre-authorised ledger rows** are in 04-VALIDATION.md. Plans 04-08 and 04-09 were each
authorised to add further rows if a correct fix would have changed behaviour — the no-op miner
cleanup, and up to three single-instance sites. Collect whatever they actually reported.

**A contradiction in the validation contract that planning found and this plan must resolve.**
04-VALIDATION.md's sign-off says `pnpm lint:ci` passes at the phase gate. Measured at plan time, the
repo carries 48 error-severity `react-hooks/rules-of-hooks` findings across 21 files, and **ten of
those files also carry bucket-C findings this phase touches**, totalling 19 inherited errors:

| File | Inherited errors |
|---|---|
| `src/components/app-handler-modal/index.tsx` | 3 |
| `src/components/embed-event/link/index.tsx` | 3 |
| `src/hooks/use-user-bookmarks-list.ts` | 4 |
| `src/components/layout/presets/app-tabs-layout.tsx` | 2 |
| `src/views/settings/accounts/components/password-signer-backup.tsx` | 2 |
| `src/components/content/components/gallery.tsx` | 1 |
| `src/components/embed-event/card/embedded-zap-receipt.tsx` | 1 |
| `src/views/feeds/dvm/index.tsx` | 1 |
| `src/views/lists/components/fallback-list-card.tsx` | 1 |
| `src/views/messages/group/index.tsx` | 1 |

Per AGENTS.md § Linting the gate fails on any error-severity finding in a touched file, so a clean
`pnpm lint:ci` is not achievable without fixing backlog 999.2 — which D-15 and the project's
no-sweep override both forbid. D-15 anticipated this but named only `app-handler-modal`. The honest
reading is that D-15's disposition extends to every inherited hook-order error in a touched file, and
this plan records that explicitly rather than letting the contract read as if the gate were clean.
Phase 3 handled the identical situation the same way.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Produce the before/after report and complete the ignores ledger</name>
  <files>.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md</files>
  <read_first>
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md (the ledger, the sampling rules and the sign-off list)
    - Every 04-NN-SUMMARY.md produced by plans 01 through 10, for the counts and any ledger rows they reported
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md § D-14
  </read_first>
  <action>
    Run the full-form per-file command and the per-rule quick counts from 04-VALIDATION.md. Assert on
    the jq output only — `aislop scan` exits non-zero whenever any finding exists anywhere in the
    repo, which is always true, so its exit code means nothing. Always redirect stderr.

    Build a fifteen-row per-rule before/after table starting from 04-01's recorded 417 baseline and
    ending at the measured final count, and attribute each rule's delta to the plans that produced it.
    Record the D-02a overlap effect explicitly: the auto-fix cleared far more than its nominal 132
    findings because `ai-slop/unused-import` and `eslint/no-unused-vars` report the same lines, and a
    larger-than-expected drop is correct rather than suspicious.

    Then take every bucket-C finding still reported and match it to a Documented Ignores Ledger row.
    Fill in the ledger in 04-VALIDATION.md with the actual per-site finding counts, adding any row
    that 04-08 or 04-09 reported. A finding with no ledger row is a D-01 failure: do not paper over
    it, record it as an open gap and say which plan should have covered it.

    Read each ledger row's reason text and judge it against D-08's bar — it must explain why the code
    could not be fixed, not merely that the behaviour is deliberate. 04-VALIDATION.md flags this as a
    manual check a rescan cannot perform. If a reason falls short, say so in the summary rather than
    silently accepting it.

    Update 04-VALIDATION.md's frontmatter and sign-off list to reflect the measured reality, ticking
    only the boxes the evidence supports.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule as $r|["eslint/no-unused-vars","ai-slop/unused-import","import/no-duplicates","ai-slop/duplicate-import","eslint/no-unused-expressions","eslint/no-unreachable","ai-slop/unreachable-code","ai-slop/empty-function","unicorn/no-useless-spread","typescript/no-unnecessary-parameter-property-assignment","eslint/no-extra-boolean-cast","unicorn/no-new-array","eslint/no-shadow-restricted-names","eslint/no-useless-rename","unicorn/no-useless-length-check"]|index($r))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs a number that exactly equals the number of findings accounted for by rows in the completed Documented Ignores Ledger.
    - 04-VALIDATION.md's ledger has a filled-in count for every row, including any row added by 04-08 or 04-09.
    - The summary contains a fifteen-row per-rule table whose "before" column sums to 417.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/narrative-comment")]|length'` outputs `21` — backlog 999.8 was never swept.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="ai-slop/swallowed-exception")]|length'` outputs `0` — Phase 3's bar held throughout.
  </acceptance_criteria>
  <done>The phase has a complete before/after report and every surviving finding is a ledgered, justified exception.</done>
</task>

<task type="auto">
  <name>Task 2: Record the inherited error-severity disposition and run the phase gates</name>
  <files>.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md</files>
  <read_first>
    - AGENTS.md § Linting (how lint:ci chooses its base and why touching a file inherits its errors)
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md § D-15
  </read_first>
  <action>
    Run `pnpm build` and confirm it passes — it is the typecheck backstop for every deletion this
    phase made, and the only mechanical check that a removed declaration was truly unreferenced.

    Run `git fetch origin next` first, then `pnpm lint:ci`, per AGENTS.md. Expect it to report
    error-severity `react-hooks/rules-of-hooks` findings in touched files.

    For every error-severity finding it reports, prove the finding predates this phase: identify the
    merge-base with `origin/next`, and use `git show` at that commit to confirm the flagged lines
    already existed in that form before any Phase 4 commit. Record a table of file, line, rule and
    the evidence. Any error-severity finding that cannot be shown to predate the phase is a real
    regression and must be investigated rather than dispositioned.

    Record the D-15 disposition explicitly and in its corrected scope: D-15 named only
    `app-handler-modal/index.tsx`, but this phase touches ten files carrying inherited hook-order
    errors, and the same disposition applies to all of them — they belong to backlog 999.2, the
    project's no-sweep override forbids fixing pre-existing findings in touched files, and fixing
    them here would be a scope change rather than hygiene. State in 04-VALIDATION.md that the phase
    gate is "lint:ci reports only inherited errors, each proven pre-existing", not "lint:ci exits
    zero", and note that this corrects the sign-off line as originally written.

    Finally, list the manual-only verifications from 04-VALIDATION.md that remain outstanding —
    notably the PoW mining spot-check from 04-08 — so phase verification surfaces them instead of
    assuming they were covered. If the dev server cannot be started, record the item as unverified
    rather than dropping it, as Phase 3 did.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm build` exits 0.
    - Every error-severity finding reported by `pnpm lint:ci` appears in the summary's table with `git show` evidence that it predates the phase.
    - `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.severity=="error")|select(.rule!="react-hooks/rules-of-hooks")]|length'` outputs `0` — the phase introduced no new error-severity rule.
    - 04-VALIDATION.md states the corrected phase-gate wording and the extended D-15 scope.
    - The summary lists every outstanding manual-only verification with its status.
  </acceptance_criteria>
  <done>Both gates are run, every inherited error is proven pre-existing, and the validation contract reflects what is actually achievable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finding count → completion claim | A count of zero can be reached by deleting load-bearing code, which is a failure that looks like success |
| inherited finding → phase regression | A touched file's pre-existing errors surface in this branch's gate and can be misread as newly introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-30 | Repudiation | declaring the phase complete on a count rather than on triage | high | mitigate | The acceptance criterion ties the surviving count to the ledger row-by-row, and an unmatched finding must be reported as an open gap rather than absorbed. Reason text is re-read against D-08's bar because a rescan cannot judge prose. |
| T-04-31 | Repudiation | an inherited hook-order error being recorded as a Phase 4 regression, or a real regression being waved through as inherited | high | mitigate | Every error-severity finding requires `git show` evidence at the merge-base; anything that cannot be shown to predate the phase is investigated, and a scan asserts no error-severity rule other than the hook-order one exists. |
| T-04-32 | Repudiation | a manual-only verification silently dropped | medium | mitigate | Outstanding manual items are listed with status; an unrunnable dev server yields an explicit unverified record, following Phase 3's precedent. |
| T-04-02 | Tampering | an ignore that over-suppresses surviving into the record unchallenged | medium | mitigate | Each ledger reason is re-read against the why-not-fixed bar, and the sqlite file-level directive's scope decision is restated so a future reader can revisit it. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed across the entire phase. |
</threat_model>

## Artifacts this phase produces

**Added**
- A completed Documented Ignores Ledger in 04-VALIDATION.md, with real per-site counts
- A corrected phase-gate definition and the extended D-15 scope in 04-VALIDATION.md
- A fifteen-row per-rule before/after report in 04-11-SUMMARY.md
- A table of inherited error-severity findings with pre-existence evidence

**Changed**
- 04-VALIDATION.md frontmatter and sign-off, reflecting measured reality

**Deliberately NOT changed**
- Any source file — this plan measures and records, it does not edit code
- Any inherited `react-hooks/rules-of-hooks` finding (backlog 999.2)

<verification>
- Surviving bucket-C findings exactly match the ledger.
- `ai-slop/narrative-comment` still reads 21 and `ai-slop/swallowed-exception` still reads 0.
- No error-severity rule other than the inherited hook-order one exists.
- `pnpm build` passes.
</verification>

<success_criteria>
The phase closes on evidence: a complete before/after table from 417, a ledger accounting for every
surviving finding with a reason that meets D-08's bar, and proof that every error the gate reports
predates the phase.
</success_criteria>

<output>
Create `.planning/phases/04-dead-code-and-import-hygiene-sweep/04-11-SUMMARY.md` when done.
</output>
</content>
