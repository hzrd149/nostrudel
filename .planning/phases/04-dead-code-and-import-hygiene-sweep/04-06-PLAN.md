---
phase: 04-dead-code-and-import-hygiene-sweep
plan: 06
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - src/views/feeds/dvm/components/dvm-avatar.tsx
  - src/views/feeds/dvm/components/dvm-params.tsx
  - src/views/feeds/relays/index.tsx
  - src/views/lists/components/user-card.tsx
  - src/views/user/components/user-card.tsx
  - src/views/relays/components/relay-card.tsx
  - src/views/notifications/focused-context.ts
  - src/views/messages/chat/components/direct-message-form.tsx
  - src/views/messages/components/direct-message-content.tsx
  - src/views/messages/group/components/group-relay-connections.tsx
  - src/views/settings/search/index.tsx
  - src/views/streams/stream/components/stream-sats-per-minute.tsx
  - src/views/thread/components/reply-form.tsx
  - src/views/tools/event-publisher/components/event-template-editor/index.tsx
autonomous: true
requirements: [D-06]
must_haves:
  truths:
    - "Every unused parameter under views/ is either underscore-prefixed because its signature is contract-bound, or deleted because it merely trails the signature (D-06)"
    - "The replyKind parameter survives as a prefixed binding — backlog 999.13 wires it up, this phase must not delete it"
    - "No caller is left passing an argument to a parameter that no longer exists"
  artifacts:
    - "14 modified source files"
  key_links:
    - "ReplyFormProps keeps its replyKind member so backlog 999.13 still has its seam"
---

<objective>
Resolve the 14 unused-parameter findings under `views/`, applying the same contract-bound versus
trailing test as 04-05 — with one site that must deliberately not be deleted.

Purpose: Completes D-06. Splitting the view files from 04-05's non-view files lets the two run in
parallel without contending for any file.
Output: 14 findings resolved across 14 files.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-CONTEXT.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md
@.planning/phases/04-dead-code-and-import-hygiene-sweep/04-VALIDATION.md
@AGENTS.md
</context>

<measured_facts>
**This phase originates the underscore convention** — there is no in-repo precedent to copy. The
shape comes from the lint message and 04-PATTERNS.md § D-06.

The 14 sites, measured at `a84d924eb`. Line numbers shift after earlier waves — locate by parameter
name within its signature.

| File | Line | Parameter |
|---|---|---|
| `src/views/feeds/dvm/components/dvm-avatar.tsx` | 16 | `noProxy` |
| `src/views/feeds/dvm/components/dvm-params.tsx` | 17 | `param` |
| `src/views/feeds/relays/index.tsx` | 93 | `showUsers` |
| `src/views/lists/components/user-card.tsx` | 16 | `relay` |
| `src/views/user/components/user-card.tsx` | 11 | `relay` |
| `src/views/relays/components/relay-card.tsx` | 108 | `to` |
| `src/views/notifications/focused-context.ts` | 3 | `id` |
| `src/views/messages/chat/components/direct-message-form.tsx` | 278 | `rootId` |
| `src/views/messages/components/direct-message-content.tsx` | 53 | `text` |
| `src/views/messages/group/components/group-relay-connections.tsx` | 10 | `group` |
| `src/views/settings/search/index.tsx` | 86 | `b` |
| `src/views/streams/stream/components/stream-sats-per-minute.tsx` | 27 | `props` |
| `src/views/thread/components/reply-form.tsx` | 30 | `replyKind` |
| `src/views/tools/event-publisher/components/event-template-editor/index.tsx` | 21 | `name` |

**`replyKind` must not be deleted.** The ROADMAP's backlog item 999.13 (NIP-22 comments in kind 1
threads) inspected this exact parameter and left a standing instruction: it is the dead write-seam
that item will wire up, and the note says explicitly that the dead-code sweep should leave it rather
than delete it. Read at plan time: `ReplyFormProps` is an exported type declaring `replyKind?:
number`, and `ReplyFormInner` destructures it with a `kinds.ShortTextNote` default that the body
then ignores. Prefix the destructured binding, keep the default, and keep the type member.

**Two parameters look contract-bound on sight.** `settings/search/index.tsx`'s `b` reads as the
second argument of a sort comparator, where position is fixed. `stream-sats-per-minute.tsx`'s
`props` is an entire unused props object — check whether the component is referenced with props
before deleting it from the signature. That file's catch clause was already handled by 04-02 in an
earlier wave; touch only the signature here.

**`settings/search/index.tsx` also carries a residual unused import** that 04-10 owns in a later
wave. Leave it.
</measured_facts>

<tasks>

<task type="auto">
  <name>Task 1: Resolve seven view parameters in feeds, lists, users, relays and notifications</name>
  <files>src/views/feeds/dvm/components/dvm-avatar.tsx, src/views/feeds/dvm/components/dvm-params.tsx, src/views/feeds/relays/index.tsx, src/views/lists/components/user-card.tsx, src/views/user/components/user-card.tsx, src/views/relays/components/relay-card.tsx, src/views/notifications/focused-context.ts</files>
  <read_first>
    - Each file being modified, reading the full signature and the call sites that reach it
    - .planning/phases/04-dead-code-and-import-hygiene-sweep/04-PATTERNS.md § D-06
  </read_first>
  <action>
    For each site, ask whether the signature is fixed by something outside the function. React
    component props destructured from a declared props type, event handlers, array callbacks and
    applesauce callbacks are contract-bound — prefix those in place. A parameter that merely trails
    the signature, where grep shows no caller passing a corresponding argument, can be deleted.

    Two different files both flag a `relay` parameter — `lists/components/user-card.tsx` and
    `user/components/user-card.tsx`. They are separate components despite the shared filename; check
    each independently rather than assuming they share a contract.

    When a flagged parameter is a destructured member of a declared props type, prefer prefixing the
    destructured binding and leaving the type member in place, unless grep shows no caller ever
    passes that prop — removing a prop from an exported props type is an API change, not hygiene.

    Where you delete a parameter, grep its call sites first and confirm none passes an argument in
    that position.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(["src/views/feeds/dvm/components/dvm-avatar.tsx","src/views/feeds/dvm/components/dvm-params.tsx","src/views/feeds/relays/index.tsx","src/views/lists/components/user-card.tsx","src/views/user/components/user-card.tsx","src/views/relays/components/relay-card.tsx","src/views/notifications/focused-context.ts"]|index(.filePath))|select(.rule=="eslint/no-unused-vars" and (.message|test("^Parameter")))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0`.
    - `pnpm build` exits 0.
    - The summary records, for every deleted parameter, that its call sites were grepped and pass no argument in that position.
  </acceptance_criteria>
  <done>Seven view parameters are resolved with contract-bound positions and exported prop types preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the remaining seven, preserving the replyKind seam</name>
  <files>src/views/messages/chat/components/direct-message-form.tsx, src/views/messages/components/direct-message-content.tsx, src/views/messages/group/components/group-relay-connections.tsx, src/views/settings/search/index.tsx, src/views/streams/stream/components/stream-sats-per-minute.tsx, src/views/thread/components/reply-form.tsx, src/views/tools/event-publisher/components/event-template-editor/index.tsx</files>
  <read_first>
    - Each file being modified, reading the full signature and its call sites
    - src/views/thread/components/reply-form.tsx lines 23-45 (the exported props type and the destructuring with its default)
    - The ROADMAP's backlog item 999.13, for the standing instruction about replyKind
  </read_first>
  <action>
    Apply the same contract-bound versus trailing test to six of these seven.

    `reply-form.tsx` is the exception and must be handled deliberately. Prefix the destructured
    `replyKind` binding so the lint rule is satisfied, and keep both the `kinds.ShortTextNote`
    default and the `replyKind` member on the exported `ReplyFormProps` type. Do not delete the
    parameter and do not remove it from the type. Backlog item 999.13 identified this as the existing
    write-seam it will wire up when NIP-22 replies land, and recorded an explicit instruction that
    this sweep should leave it in place. Note in the summary that this site was preserved by that
    instruction rather than resolved on its own merits.

    `settings/search/index.tsx`'s `b` is almost certainly a sort comparator's second argument — a
    fixed position, so prefix rather than delete. Leave that file's residual unused import for 04-10.

    `stream-sats-per-minute.tsx`'s `props` is a whole unused props object. Check how the component is
    rendered before removing it from the signature; if any call site passes props, prefix instead.
    Its catch clause was already converted by 04-02, so change only the signature.

    Where you delete a parameter, grep its call sites first.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]|select(.rule=="eslint/no-unused-vars" and (.message|test("^Parameter")))]|length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs `0` — every unused parameter repo-wide is resolved, closing D-06 in full.
    - `grep -c "replyKind" src/views/thread/components/reply-form.tsx` outputs a value of at least `2` — the type member and the prefixed binding both survive.
    - `grep -c "ShortTextNote" src/views/thread/components/reply-form.tsx` outputs `1` — the default is intact.
    - `pnpm build` exits 0.
    - The summary states that `replyKind` was preserved under backlog 999.13's instruction.
  </acceptance_criteria>
  <done>All 14 view parameters are resolved, D-06 is closed repo-wide, and backlog 999.13's write-seam is intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| function signature → caller | Deleting a positional parameter silently changes what later arguments bind to |
| this phase → backlog 999.13 | One flagged parameter is a deliberate seam another item will wire up |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-22 | Tampering | deleting a contract-bound parameter and shifting later positional arguments | high | mitigate | The contract-bound test is applied per site; call sites are grepped before any deletion and `pnpm build` type-checks every remaining caller. |
| T-04-25 | Repudiation | deleting `replyKind` and destroying backlog 999.13's write-seam | high | mitigate | The action forbids deletion and requires keeping the type member and default; two grep acceptance criteria assert both survive. Neither `tsc` nor the linter would flag this loss, which is why it is asserted explicitly. |
| T-04-26 | Tampering | removing a prop from an exported props type as if it were hygiene | medium | mitigate | Prop members are kept unless grep shows no caller passes them; removing one is treated as an API change and out of scope for this phase. |
| T-04-SC | Tampering | npm/pip/cargo installs | low | accept | No packages installed. |
</threat_model>

## Artifacts this phase produces

**Changed**
- 14 parameters across 14 view files, each either underscore-prefixed in place or deleted
- `reply-form.tsx`'s `replyKind` prefixed but otherwise fully preserved

**Deliberately NOT changed**
- `ReplyFormProps.replyKind` and its `kinds.ShortTextNote` default (backlog 999.13)
- The residual unused import in `settings/search/index.tsx` (04-10)
- The catch clause in `stream-sats-per-minute.tsx` (already handled by 04-02)

<verification>
- Unused-parameter findings repo-wide: 0, closing D-06.
- `replyKind` and its default are still present in `reply-form.tsx`.
- `pnpm build` passes after each task.
</verification>

<success_criteria>
Every unused parameter under `views/` is resolved by the remedy its signature calls for, and the one
site another backlog item depends on is preserved rather than swept away.
</success_criteria>

<output>
Create `.planning/phases/04-dead-code-and-import-hygiene-sweep/04-06-SUMMARY.md` when done.
</output>
</content>
