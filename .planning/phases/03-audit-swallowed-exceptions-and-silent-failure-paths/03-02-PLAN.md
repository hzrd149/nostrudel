---
phase: 03-audit-swallowed-exceptions-and-silent-failure-paths
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/helpers/parse.ts
  - src/helpers/nip19.ts
  - src/components/content/transform/bip-notation.ts
  - src/components/content/transform/nip-notation.ts
  - src/helpers/nostr/goal.ts
  - src/hooks/use-open-graph-data.ts
  - src/views/tools/event-publisher/index.tsx
  - src/views/wallet/components/receive-token-modal.tsx
  - src/views/streams/stream/components/stream-top-zappers.tsx
  - src/components/app-handler-modal/index.tsx
  - src/components/relay-url-input.tsx
  - src/views/lists/components/list-history-modal.tsx
  - src/views/settings/cache/database/components/import-events-button.tsx
autonomous: true
requirements: [D-01, D-05, D-06, D-08]

must_haves:
  truths:
    - "All thirteen parse/filter guard sites carry zero error-severity bucket-B findings"
    - "Every rewritten catch states what failed and what the caller does with the absent value"
    - "Each guard returns or continues with the exact value its fall-through produced before the edit"
    - "The zap-totals reducer still returns its accumulator when a zap cannot be read"
  artifacts:
    - path: "src/helpers/parse.ts"
      provides: "safeUrl guard with reason comment and explicit return"
      contains: "return undefined"
    - path: "src/views/streams/stream/components/stream-top-zappers.tsx"
      provides: "reduce guard returning the accumulator, not undefined"
      contains: "return dir"
    - path: "src/views/settings/cache/database/components/import-events-button.tsx"
      provides: "per-line JSON parse guard with explicit continue"
      contains: "continue"
  key_links:
    - from: "src/helpers/nip19.ts"
      to: "src/helpers/nip19.ts"
      via: "normalizeToHexPubkey consumes safeDecode's undefined return as 'not decodable'"
      pattern: "safeDecode"
---

<objective>
Apply D-05's default parse-guard remedy to the thirteen sites whose enclosing control flow permits an
explicit exit: a reason comment (D-08) plus an explicit `return`/`continue`, with the caught binding
dropped to the bare `catch {` form (D-06).

Purpose: These thirteen account for 13 of the 31 error-severity findings (D-01). Each is a genuine
parse or filter guard — nothing is failing that the caller cares about — but a comment alone does not
clear `ai-slop/swallowed-exception`, so each needs real control flow added.

Output: Thirteen modified files, zero new files, zero new symbols, zero behavior change.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-CONTEXT.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-RESEARCH.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-PATTERNS.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-VALIDATION.md
@AGENTS.md

<measured_facts>
<!-- Measured live against aislop@0.16.1 during planning. Do not re-derive. -->

1. A reason comment ALONE never clears `ai-slop/swallowed-exception` — verified live on a disposable
   probe, twice. `src/components/relay-url-input.tsx:62` is the in-repo proof: it already carries a
   good comment and still fires.
2. An explicit `return`/`continue` inside the catch body clears it.
3. The bucket-B error-severity target is **31 across 30 files**, not the "32 across 33" in CONTEXT.md
   D-01/D-04. `ai-slop/silent-recovery` is warning-severity.
4. Dropping an unused binding to bare `catch {` also clears the `eslint/no-unused-vars` warning that
   sits on the same line (D-06).
</measured_facts>

<remedy_table>
<!-- The exact exit statement each site needs. Derived by reading every enclosing construct. -->

| File | Catch line | Current binding | Exit statement to add |
|------|-----------|-----------------|----------------------|
| src/helpers/parse.ts | 4 | `e` | `return undefined;` |
| src/helpers/nip19.ts | 11 | `e` | `return undefined;` |
| src/components/content/transform/bip-notation.ts | 42 | `error` | `return false;` |
| src/components/content/transform/nip-notation.ts | 42 | `error` | `return false;` |
| src/helpers/nostr/goal.ts | 105 | `e` | `return false;` |
| src/hooks/use-open-graph-data.ts | 34 | `e` | `return null;` |
| src/views/tools/event-publisher/index.tsx | 74 | `error` | `return;` |
| src/views/wallet/components/receive-token-modal.tsx | 36 | `error` | `return trimmed;` |
| src/views/streams/stream/components/stream-top-zappers.tsx | 19 | `error` | `return dir;` |
| src/components/app-handler-modal/index.tsx | 138 | `error` | `return false;` |
| src/components/relay-url-input.tsx | 62 | `err` | `return;` |
| src/views/lists/components/list-history-modal.tsx | 319 | `error` | `return;` |
| src/views/settings/cache/database/components/import-events-button.tsx | 22 | `e` | `continue;` |

Every exit statement above reproduces the value the site already fell through to, so behavior is
unchanged at all thirteen. In every case the binding is unused after the edit, so all thirteen become
bare `catch {` per D-06.
</remedy_table>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Parse guards in helpers, content transforms and hooks (6 files)</name>
  <files>src/helpers/parse.ts, src/helpers/nip19.ts, src/components/content/transform/bip-notation.ts, src/components/content/transform/nip-notation.ts, src/helpers/nostr/goal.ts, src/hooks/use-open-graph-data.ts</files>
  <read_first>
    - src/helpers/parse.ts — 5 lines, read in full
    - src/helpers/nip19.ts — read `safeDecode` and its consumer `normalizeToHexPubkey` (lines 1-20)
    - src/components/content/transform/bip-notation.ts — read the transformer callback around lines 25-48
    - src/components/content/transform/nip-notation.ts — read the transformer callback around lines 25-48
    - src/helpers/nostr/goal.ts — read `validateGoal` and `safeValidateGoal` (lines 90-108)
    - src/hooks/use-open-graph-data.ts — read the fetch callback (lines 18-37)
  </read_first>
  <action>
    Apply the D-05 remedy at each of the six catch sites listed in the remedy table above, using that
    table's exact exit statement. For each site: convert the catch to the bare `catch {` form (D-06 —
    every binding here is unused), write a reason comment that names what failed and what the caller
    does with the absent value (D-08), then add the exit statement.

    Per-site notes that determine the correct wording and value:

    `src/helpers/parse.ts:4` — `safeUrl` wraps `new URL(url).toString()`. The comment should say the
    input is not a parseable URL and that callers treat `undefined` as "no URL". This file is the
    shape the whole D-05 pattern was probed against.

    `src/helpers/nip19.ts:11` — `safeDecode` wraps `nip19.decode`. Its consumer
    `normalizeToHexPubkey` (same file) treats a falsy result as "not decodable", so say that.

    `src/components/content/transform/bip-notation.ts:42` and
    `src/components/content/transform/nip-notation.ts:42` — both are inside a content-transformer
    callback whose next statement is already `return false;` (line 44), meaning "this match is not a
    BIP/NIP reference, leave the text alone". Return `false` from the catch so the behavior is
    identical. Do not merge or refactor these two near-identical files — that is Phase 5's concern.

    `src/helpers/nostr/goal.ts:105` — `safeValidateGoal` wraps `validateGoal`, which throws on a
    malformed goal event. The next statement is already `return false;`. Say the goal event failed
    validation and callers filter it out.

    `src/hooks/use-open-graph-data.ts:34` — the fetch/parse path; the next statement is already
    `return null;`. Say the page could not be fetched or had no parseable Open Graph tags and callers
    render no preview.

    Do not touch any other finding in these files. Do not add a logger to any of them — an explicit
    return is the D-05 default and no log is warranted for a pure parse guard.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.severity=="error") | select(.rule=="ai-slop/swallowed-exception" or .rule=="ai-slop/silent-recovery") | select(.filePath | test("^src/(helpers/parse\\.ts|helpers/nip19\\.ts|helpers/nostr/goal\\.ts|hooks/use-open-graph-data\\.ts|components/content/transform/(bip|nip)-notation\\.ts)$"))] | length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs exactly `0` (it reads `6` before this task). Assert on jq output only — `aislop scan` exits 1 whenever any finding exists anywhere in the repo.
    - `grep -c 'return undefined' src/helpers/parse.ts` outputs `1`
    - `grep -c 'return undefined' src/helpers/nip19.ts` outputs `1`
    - `grep -c 'return false' src/components/content/transform/bip-notation.ts` outputs `3` — the pre-existing early return, the transformer's fall-through, and the new one in the catch
    - `grep -c 'return false' src/components/content/transform/nip-notation.ts` outputs `3`
    - `grep -c 'return false' src/helpers/nostr/goal.ts` outputs at least `2`
    - `grep -c 'return null' src/hooks/use-open-graph-data.ts` outputs at least `2`
    - `pnpm build` completes successfully
  </acceptance_criteria>
  <done>All six files produce zero error-severity bucket-B findings; each guard returns the same value its fall-through produced before the edit.</done>
</task>

<task type="auto">
  <name>Task 2: Filter and loop guards in views and components (7 files)</name>
  <files>src/views/tools/event-publisher/index.tsx, src/views/wallet/components/receive-token-modal.tsx, src/views/streams/stream/components/stream-top-zappers.tsx, src/components/app-handler-modal/index.tsx, src/components/relay-url-input.tsx, src/views/lists/components/list-history-modal.tsx, src/views/settings/cache/database/components/import-events-button.tsx</files>
  <read_first>
    - src/views/tools/event-publisher/index.tsx — read the `useEffect` around lines 68-76
    - src/views/wallet/components/receive-token-modal.tsx — read `normalizeToken` (lines 30-38)
    - src/views/streams/stream/components/stream-top-zappers.tsx — read the `reduce` callback (lines 11-24)
    - src/components/app-handler-modal/index.tsx — read the `filteredApps` filter chain (lines 128-145)
    - src/components/relay-url-input.tsx — read `normalizeValue` (lines 50-66)
    - src/views/lists/components/list-history-modal.tsx — read the `unlock` useCallback including its `finally` block (lines 310-331)
    - src/views/settings/cache/database/components/import-events-button.tsx — read the reader.onload handler (lines 12-28)
  </read_first>
  <action>
    Apply the D-05 remedy at each of the seven catch sites listed in the remedy table above, using that
    table's exact exit statement. Same recipe: bare `catch {` (D-06), reason comment naming the failure
    and the caller's handling of the absent value (D-08), then the exit statement.

    Three of these have a control-flow subtlety the executor must get right:

    `src/views/streams/stream/components/stream-top-zappers.tsx:19` — this catch is inside a
    `zaps.reduce<Record<string, number>>((dir, z) => {...})` callback. The exit statement MUST be
    `return dir;`, returning the accumulator. A bare `return;` would make the accumulator `undefined`
    on the next iteration and crash the component. The comment should say the zap could not be read
    (`getZapSender`/`getZapPayment` threw) and is left out of the totals.

    `src/views/settings/cache/database/components/import-events-button.tsx:22` — this catch is inside
    `for (const line of lines)`, so the loop equivalent of a return is `continue;`. The comment should
    say the line was not parseable JSON and is skipped. Note the importer then reports
    `Imported ${events.length} events`, silently excluding skipped lines — making that count honest is
    an explicitly deferred idea in CONTEXT.md, so do NOT change the alert text or add a skipped-line
    count here.

    `src/views/lists/components/list-history-modal.tsx:319` — this catch is followed by a `finally`
    block (lines 321-327) that removes the event id from `unlockingIds`. A `return;` inside the catch
    still runs that `finally`, and nothing follows the try/catch/finally in the arrow function, so
    `return;` is safe. Keep the `finally` block exactly as written. The existing comment ("ignore —
    the signer was denied or the content could not be decrypted") already names what failed; extend it
    to also say the row simply stays locked, so it satisfies D-08's "what the caller does with the
    absent value" half.

    The remaining four are straightforward:

    `src/views/tools/event-publisher/index.tsx:74` — the catch is the last statement in a `useEffect`
    body, so `return;` is safe. Say the draft could not be processed into an unsigned event and
    `processed` is left unset.

    `src/views/wallet/components/receive-token-modal.tsx:36` — `normalizeToken`'s next statement is
    `return trimmed;`. Return `trimmed` from the catch (it is in scope from line 32) so the pasted
    value is passed through unchanged when it is not an emoji-encoded token.

    `src/components/app-handler-modal/index.tsx:138` — inside a `.filter()` callback whose next
    statement is `return false;`. Say the app's profile content could not be parsed so it is excluded
    from search results.

    `src/components/relay-url-input.tsx:62` — the catch is the last statement in `normalizeValue`, so
    `return;` is safe. This file already has a correct reason comment ("Ignore invalid URLs, let form
    validation handle them") — keep it and add the `return;`. This is the site research flagged as the
    trap case: a good comment that still fires. The file's early `if (!value) return;` on line 54 shows
    the bare-return idiom already used here.

    Do not touch any other finding in these files — `relay-url-input.tsx` in particular carries
    unrelated `jsx-a11y` findings that belong to Phase 7.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.severity=="error") | select(.rule=="ai-slop/swallowed-exception" or .rule=="ai-slop/silent-recovery") | select(.filePath | test("^src/(views/tools/event-publisher/index\\.tsx|views/wallet/components/receive-token-modal\\.tsx|views/streams/stream/components/stream-top-zappers\\.tsx|components/app-handler-modal/index\\.tsx|components/relay-url-input\\.tsx|views/lists/components/list-history-modal\\.tsx|views/settings/cache/database/components/import-events-button\\.tsx)$"))] | length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs exactly `0` (it reads `7` before this task)
    - `grep -c 'return dir' src/views/streams/stream/components/stream-top-zappers.tsx` outputs `2` — the reducer's normal return plus the new accumulator return inside the catch
    - `grep -c 'continue' src/views/settings/cache/database/components/import-events-button.tsx` outputs at least `1`
    - `grep -c 'Imported ${events.length} events' src/views/settings/cache/database/components/import-events-button.tsx` outputs `1` — the deferred honest-count change was not made
    - `grep -c 'return trimmed' src/views/wallet/components/receive-token-modal.tsx` outputs `2`
    - `grep -c 'finally' src/views/lists/components/list-history-modal.tsx` outputs at least `2` — both finally blocks preserved
    - `grep -c 'let form validation handle them' src/components/relay-url-input.tsx` outputs `1` — the existing reason comment was kept, not replaced
    - `pnpm build` completes successfully
  </acceptance_criteria>
  <done>All seven files produce zero error-severity bucket-B findings; the reducer still returns its accumulator, the import loop still advances, and the unlock cleanup still runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| remote event / relay content → parse guards | Untrusted nostr event tags, nip19 strings, URLs and pasted tokens cross into parsers |
| user-supplied file → import-events-button | Untrusted newline-delimited JSON is parsed per line |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-05 | Tampering | All thirteen parse/filter guards | low | accept | The guards' behavior is unchanged: malformed input is still rejected and still treated as absent. This plan only makes the rejection explicit in source. No validation logic is added, removed or loosened. |
| T-03-06 | Denial of Service | `stream-top-zappers.tsx` reduce accumulator | medium | mitigate | A bare `return;` in a reduce callback would yield an `undefined` accumulator and crash the component on the next iteration. The remedy table mandates `return dir;` and an acceptance criterion asserts the accumulator return is present, so the correct form is enforced rather than left to the executor's judgment. |
| T-03-07 | Information Disclosure | Reason comments (D-08) | low | accept | Comments describe failure classes only. No secret, key or user content is named. |

No new attack surface: no network call, parser, dependency or exported API is added. ASVS L1 — these
are not primary trust boundaries beyond the input validation already in place.
</threat_model>

<verification>
```bash
# All thirteen files in this plan must contribute zero error-severity bucket-B findings.
pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]
  | select(.severity=="error")
  | select(.rule=="ai-slop/swallowed-exception" or .rule=="ai-slop/silent-recovery")
  | select(.filePath | test("^src/(helpers/parse\\.ts|helpers/nip19\\.ts|helpers/nostr/goal\\.ts|hooks/use-open-graph-data\\.ts|components/content/transform/(bip|nip)-notation\\.ts|views/tools/event-publisher/index\\.tsx|views/wallet/components/receive-token-modal\\.tsx|views/streams/stream/components/stream-top-zappers\\.tsx|components/app-handler-modal/index\\.tsx|components/relay-url-input\\.tsx|views/lists/components/list-history-modal\\.tsx|views/settings/cache/database/components/import-events-button\\.tsx)$"))]
  | length'
# expect: 0   (reads 13 before this plan)

pnpm build
```

This plan runs in parallel with 03-03, 03-04 and 03-05, so do NOT assert on the whole-repo bucket-B
total here — a sibling plan landing concurrently would make that number move unpredictably. Assert on
this plan's own file set. Never assert on the scan's exit code.
</verification>

<success_criteria>
- All thirteen files produce zero error-severity bucket-B findings
- This plan removes 13 of the 31 errors
- `pnpm build` passes
- Every rewritten catch has both a reason comment and an explicit exit statement
- No unrelated finding in a touched file was swept
</success_criteria>

## Artifacts this phase produces

This plan creates **no new files, no new exported symbols, and no new module-scope identifiers**. It is
thirteen in-place catch-block rewrites. Nothing is added to any module's public surface; nothing is
removed; no signature changes.

<output>
Create `.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-02-SUMMARY.md` when done.
Record which exit statement landed at each of the thirteen sites so plan 03-06's D-04 table can cite them.
</output>
