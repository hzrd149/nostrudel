---
phase: 03-audit-swallowed-exceptions-and-silent-failure-paths
plan: 05
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/services/sqlite/index.ts
  - src/components/qr-code/native-scanner.ts
  - src/hooks/timeline/use-timeline-cache-key.ts
  - src/index.tsx
autonomous: true
requirements: [D-01, D-06, D-07, D-12, D-13, D-14, D-15]

must_haves:
  truths:
    - "All four warning-severity bucket-B strays are cleared"
    - "src/index.tsx carries zero error-severity bucket-B findings and no console-leftover finding"
    - "The sqlite module's two ceremonial try/catch wrappers are gone and both functions still reject on failure"
    - "The barcode-listener promise no longer uses an async executor and still resolves, rejects and removes its listener"
    - "Phase 4's territory in sqlite/index.ts (the module-level throw and the unreachable code beneath it) is untouched"
  artifacts:
    - path: "src/services/sqlite/index.ts"
      provides: "openConnection and deleteDatabase without redundant try/catch ceremony"
      contains: "export async function openConnection"
    - path: "src/components/qr-code/native-scanner.ts"
      provides: "listener registration hoisted out of the Promise constructor"
      contains: "new Promise<void>"
    - path: "src/hooks/timeline/use-timeline-cache-key.ts"
      provides: "rule-scoped ignore documenting the hidden-fallback false positive"
      contains: "aislop-ignore-next-line ai-slop/hidden-fallback"
    - path: "src/index.tsx"
      provides: "protocol-handler registration failure logged with its cause via the namespaced logger"
      contains: "logger.extend(\"Index\")"
  key_links:
    - from: "src/index.tsx"
      to: "src/helpers/debug.ts"
      via: "logger already imported on line 6; extended for the protocol-handler catch"
      pattern: "logger.extend"
---

<objective>
Clear the four warning-severity strays that make up the remainder of bucket B (D-13), plus the one
error-severity finding bundled with them at `src/index.tsx:49` (D-15).

Purpose: D-13 put these in scope because finishing them closes bucket B rather than leaving an awkward
remainder. Three are one- or two-line changes; `native-scanner.ts:15` is a genuine small refactor and
gets its own task. `src/index.tsx:49` is the single line where an error-severity
`swallowed-exception`, a warning-severity `silent-recovery`, an unused binding and a
`console-leftover` all sit together — one edit clears all four (D-15, D-12, D-06).

Output: Four modified files, zero new files, one new module-private logger constant.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-CONTEXT.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-RESEARCH.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-PATTERNS.md
@.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-VALIDATION.md
@AGENTS.md

<interfaces>
From src/helpers/debug.ts (the entire file — 3 lines):
```typescript
import debug from "debug";

export const logger = debug("noStrudel");
```
`src/index.tsx` ALREADY imports `logger` on line 6 and already calls it directly on line 63
(`logger("Rendering app")`). No new import is needed there — only a `logger.extend(...)` constant.

The only rule-scoped ignore that exists in the repo today, `src/sw/client/error-logger.ts:1`, is the
syntax model for D-14's directive:
```typescript
// aislop-ignore-file ai-slop/console-leftover -- this module's purpose is console output
```
Per `AGENTS.md` "#### Inline ignores", a directive MUST name the rule and MUST end with `-- reason`.
</interfaces>

<measured_facts>
<!-- Measured live against aislop@0.16.1 during planning. Do not re-derive. -->

1. Exact current findings in the four files:

| File | Line | Severity | Rule | Owner |
|------|------|----------|------|-------|
| src/services/sqlite/index.ts | 39 | warning | `ai-slop/redundant-try-catch` | **this plan** |
| src/services/sqlite/index.ts | 54 | warning | `ai-slop/redundant-try-catch` | **this plan** |
| src/services/sqlite/index.ts | 9 | warning | `ai-slop/unreachable-code` | Phase 4 — do not touch |
| src/services/sqlite/index.ts | 9,10,12,13,14,15 | warning | `eslint/no-unreachable` (×6) | Phase 4 — do not touch |
| src/services/sqlite/index.ts | 48 | warning | `eslint/no-unused-vars` (`dbName`) | Phase 4 — do not touch |
| src/components/qr-code/native-scanner.ts | 15 | warning | `eslint/no-async-promise-executor` | **this plan** |
| src/hooks/timeline/use-timeline-cache-key.ts | 14 | warning | `ai-slop/hidden-fallback` | **this plan** |
| src/hooks/timeline/use-timeline-cache-key.ts | 11 | warning | `react-hooks/exhaustive-deps` | backlog 999.5 — do not touch |
| src/index.tsx | 49 | **error** | `ai-slop/swallowed-exception` | **this plan** |
| src/index.tsx | 49 | warning | `ai-slop/silent-recovery` | **this plan** |
| src/index.tsx | 49 | warning | `eslint/no-unused-vars` (`e`) | **this plan**, cleared in passing (D-06) |
| src/index.tsx | 50 | warning | `ai-slop/console-leftover` | **this plan**, cleared in passing (D-15) |
| src/index.tsx | 18,34,41,54 | warning | `ai-slop/trivial-comment` (×4) | backlog 999.8 — do not touch |

2. `ai-slop/silent-recovery` is **warning**-severity, not error. CONTEXT.md D-01/D-04 call the target
   "32 across 33"; the true error-severity target is **31 across 30 files**. This plan contributes
   exactly 1 of those 31 (`index.tsx:49`); its other three files are warning-only.
3. `aislop-ignore-next-line` must sit on the line immediately above the flagged line. A directive
   placed earlier in the enclosing block does NOT suppress. (Research also established that
   `aislop-ignore-file` works from anywhere in a file, correcting CONTEXT.md's probe table — not
   needed here, since D-14 uses the `-next-line` form.)
</measured_facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the two ceremonial try/catch wrappers in the sqlite service</name>
  <files>src/services/sqlite/index.ts</files>
  <read_first>
    - src/services/sqlite/index.ts — read the whole file (57 lines) before editing; note the module-level throw on line 8 and the code beneath it, which belongs to Phase 4
  </read_first>
  <action>
    Both findings are `catch (err) { return Promise.reject(err); }` inside functions that are already
    declared `async`. An `async` function already returns a rejected promise for any uncaught throw, so
    the wrapper adds nothing — the fix is deletion, not annotation.

    In `openConnection` (lines 18-42): delete the `try {` on line 26 and the entire catch block on
    lines 39-41, and dedent the body (lines 27-38) one level. Keep the
    `let db: SQLiteDBConnection;` declaration on line 25 — it is still assigned in both branches of the
    if/else — and keep `return db;` as the function's last statement.

    In `deleteDatabase` (lines 44-57): delete the `try {` on line 45 and the entire catch block on
    lines 54-56, and dedent the body (lines 46-53) one level. Keep both
    `return Promise.resolve();` statements and the `if (ret) / else` structure exactly as they are —
    do not collapse or simplify the branches, which would be scope creep into Phase 5's territory.

    Two hard boundaries, both because `src/services/sqlite/index.ts` is shared with Phase 4:
    - Do NOT touch lines 1-16. The module-level `throw new Error("Do not load the sqlite module on
      web...")` on line 8 and the deliberately unreachable code beneath it (lines 9-15) are Phase 4's
      explicit target. Scope this diff to lines 18-57 only.
    - Do NOT remove the unused `const dbName = db.getConnectionDBName();` on line 48. It carries a
      Phase 4 `eslint/no-unused-vars` finding; removing it here would be a drive-by sweep, which
      `.claude/CLAUDE.md` forbids.

    Behavior is identical after this edit: a throw from any awaited call inside either function still
    produces a rejected promise to the caller.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/services/sqlite/index.ts") | select(.rule=="ai-slop/redundant-try-catch")] | length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs exactly `0` (it reads `2` before this task). Assert on jq output only — `aislop scan` exits 1 whenever any finding exists anywhere in the repo.
    - This command still outputs `6`, proving Phase 4's unreachable-code region was not touched: `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/services/sqlite/index.ts") | select(.rule=="eslint/no-unreachable")] | length'`
    - This command still outputs `1`, proving the `dbName` binding was left for Phase 4: `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/services/sqlite/index.ts") | select(.rule=="eslint/no-unused-vars")] | length'`
    - `grep -c 'Do not load the sqlite module on web' src/services/sqlite/index.ts` outputs `1` — the module-level guard is intact
    - `grep -c 'return Promise.reject' src/services/sqlite/index.ts` outputs `0` — both ceremonial rejections are gone
    - `grep -c 'return Promise.resolve();' src/services/sqlite/index.ts` outputs `2` — both real resolve paths preserved
    - `grep -c 'return db;' src/services/sqlite/index.ts` outputs `1`
    - `pnpm build` completes successfully
  </acceptance_criteria>
  <done>Both `redundant-try-catch` findings are gone, both functions still reject on failure, and every Phase 4 finding in the file is still present and untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Hoist the barcode listener registration out of the Promise executor</name>
  <files>src/components/qr-code/native-scanner.ts</files>
  <read_first>
    - src/components/qr-code/native-scanner.ts — read the whole file (88 lines); `installNativeScanner` is lines 8-55 and the async executor is line 15
  </read_first>
  <action>
    This is the one site in the phase that is a real refactor rather than a mechanical rewrite, so treat
    it with a larger review surface than the other three strays.

    Why the executor is currently `async`: `sub` — the listener handle the callback needs in order to
    call `sub.remove()` on itself — is only available after `await`ing
    `BarcodeScanner.addListener(...)`, and that registration happens inside the `Promise` constructor.
    `eslint/no-async-promise-executor` fires because an async executor's own thrown errors cannot reach
    the promise's `reject`. This code does not currently depend on that failure mode (it calls `rej(...)`
    explicitly), so the rule is flagging a latent hazard, not an active bug — which is why the fix must
    not change observable behavior.

    Restructure line 15's `await new Promise<void>(async (res, rej) => {...})` as follows:
    - Drop `async` from the executor so it is a plain `(res, rej) => {...}`.
    - Declare a mutable `sub` handle inside the executor, initially undefined, typed so the file still
      compiles — `Awaited<ReturnType<typeof BarcodeScanner.addListener>>` is the natural spelling, but
      `BarcodeScanner.addListener` is overloaded, so if `tsc` rejects that form use whichever
      annotation compiles cleanly. `pnpm build` is the arbiter here; do not reach for `any` or a
      `@ts-ignore`, since Phase 6 exists to remove exactly those escape hatches.
    - Call `BarcodeScanner.addListener("googleBarcodeScannerModuleInstallProgress", ...)` without
      `await` and assign the handle in a `.then()` continuation on the returned promise.
    - Inside the event callback, change the three terminal cases to use optional-call removal
      (`sub?.remove()`), because the handle may not be assigned yet on a very early event. Keep all
      three terminal cases and their exact semantics: `COMPLETED` removes and resolves; `FAILED`
      removes and rejects with `new Error("Failed to install")`; `CANCELED` removes and rejects with
      `new Error("Canceled install")`.
    - Keep every progress-logging branch exactly as written — the `log("Installing google barcode
      scanner", event.progress)` call at the top of the callback and the `PENDING`, `DOWNLOADING`,
      `DOWNLOAD_PAUSED` and `INSTALLING` cases. That is six `log(` call sites in the file in total; all
      must survive.
    - Leave the rest of the file completely alone: the `isGoogleBarcodeScannerModuleAvailable` check,
      the `isSupported`/`requestPermissions` block on lines 48-54, and the four other exported
      functions (`getScanningStream`, `startScanning`, `stopScanning`, `scanSingle`).

    This module already imports `logger` and defines `const log = logger.extend("NativeQrCodeScanner");`
    on line 6 — add no new logger.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/components/qr-code/native-scanner.ts") | select(.rule=="eslint/no-async-promise-executor")] | length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs exactly `0` (it reads `1` before this task)
    - `pnpm build` completes successfully — this is the primary correctness gate for this task, since the file is native-only code that cannot be exercised in the web dev server
    - `grep -c 'new Promise<void>' src/components/qr-code/native-scanner.ts` outputs `1` — the promise wrapper still exists and was not removed wholesale
    - `grep -c 'log(' src/components/qr-code/native-scanner.ts` outputs `6` — every progress log survived the restructure
    - `grep -c 'Failed to install' src/components/qr-code/native-scanner.ts` outputs `1`
    - `grep -c 'Canceled install' src/components/qr-code/native-scanner.ts` outputs `1`
    - `grep -c 'sub?.remove()' src/components/qr-code/native-scanner.ts` outputs `3` — all three terminal cases remove the listener
    - `grep -c 'Camera access denied' src/components/qr-code/native-scanner.ts` outputs `1` — the permissions block below the promise is untouched
    - This command outputs `0`, proving no type-safety escape hatch was used: `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/components/qr-code/native-scanner.ts") | select(.rule=="ai-slop/unsafe-type-assertion" or .rule=="ai-slop/ts-directive" or .rule=="ai-slop/double-type-assertion")] | length'`
  </acceptance_criteria>
  <done>The async-promise-executor finding is gone, the listener is registered outside the Promise constructor, all three terminal cases still remove the listener and settle the promise, and the file typechecks.</done>
</task>

<task type="auto">
  <name>Task 3: Document the timeline cache-key false positive and log the protocol-handler failure</name>
  <files>src/hooks/timeline/use-timeline-cache-key.ts, src/index.tsx</files>
  <read_first>
    - src/hooks/timeline/use-timeline-cache-key.ts — read the whole file (15 lines)
    - src/index.tsx — read the whole file (73 lines); `logger` is imported on line 6, the target catch is at 49-51
    - src/sw/client/error-logger.ts — read line 1, the repo's only existing rule-scoped ignore, as the syntax model
    - AGENTS.md — the "#### Inline ignores" section (rule name plus `-- reason` is mandatory)
  </read_first>
  <action>
    **src/hooks/timeline/use-timeline-cache-key.ts** — D-14 classifies line 14's
    `ai-slop/hidden-fallback` as a false positive, and D-07 makes a rule-scoped ignore the last-resort
    remedy for exactly this case. Insert a single `aislop-ignore-next-line` comment on the line
    immediately above `return cacheKey || fallback;` (currently line 14) — the directive must be
    textually adjacent to the flagged line, since one placed earlier in the function does not suppress.

    The directive must name the rule `ai-slop/hidden-fallback` and end with `-- reason`. D-07 requires
    the reason to justify why the code could not be fixed instead, not merely that the behavior is
    deliberate, so the reason must convey: `fallback` is a stable `nanoid` produced by `useMemo` that
    serves the first render until the effect on lines 10-12 writes it into route state, so this
    expression is initialization rather than error recovery — there is no failure path here to make
    explicit. Keep the code itself exactly as it is.

    Do not touch line 11's `react-hooks/exhaustive-deps` warning — that belongs to backlog 999.5. Add
    no second directive.

    **src/index.tsx** — D-15. `logger` is already imported on line 6, so add only
    `const log = logger.extend("Index");` at module scope, placed immediately above the
    `if (import.meta.env.PROD && CAP_IS_WEB) {` block on line 46 so it sits beside its only consumer.

    Then rewrite the catch on lines 49-51 so that it names the failure and passes the caught error to
    the namespaced logger: keep a binding (rename `e` to `error` for clarity) and call
    `log("Failed to register web+nostr protocol handler", error)`. One edit clears four findings at
    once — the error-severity `swallowed-exception`, the warning `silent-recovery` (which fires
    precisely because the current call logs a bare string and drops the cause), the
    `eslint/no-unused-vars` on the unused binding (D-06), and the `ai-slop/console-leftover` on line 50
    (D-15, in passing).

    Registering the `web+nostr` protocol handler is genuinely optional, so add no toast and no other
    user-facing surfacing — D-15 explicitly settled this. Route the message through the namespaced
    logger only; D-12 rejected the direct-console channel and Phase 2 (D-07) keeps
    `ai-slop/console-leftover` on.

    Leave the four `ai-slop/trivial-comment` findings at lines 18, 34, 41 and 54 alone (backlog 999.8),
    and leave `logger("Rendering app")` on line 63 as it is.
  </action>
  <verify>
    <automated>pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/index.tsx" or .filePath=="src/hooks/timeline/use-timeline-cache-key.ts") | select(.rule=="ai-slop/swallowed-exception" or .rule=="ai-slop/silent-recovery" or .rule=="ai-slop/hidden-fallback" or .rule=="ai-slop/console-leftover")] | length'</automated>
  </verify>
  <acceptance_criteria>
    - The jq command above outputs exactly `0` (it reads `4` before this task: one swallowed-exception, one silent-recovery, one console-leftover, one hidden-fallback)
    - `grep -c 'aislop-ignore-next-line ai-slop/hidden-fallback' src/hooks/timeline/use-timeline-cache-key.ts` outputs `1`
    - `grep -c ' -- ' src/hooks/timeline/use-timeline-cache-key.ts` outputs `1` — the directive carries the mandatory reason delimiter
    - `grep -c 'return cacheKey || fallback;' src/hooks/timeline/use-timeline-cache-key.ts` outputs `1` — the code itself is unchanged
    - This command still outputs `1`, proving backlog 999.5's finding was not swept: `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/hooks/timeline/use-timeline-cache-key.ts") | select(.rule=="react-hooks/exhaustive-deps")] | length'`
    - `grep -c 'logger.extend("Index")' src/index.tsx` outputs `1`
    - `grep -c 'log("Failed to register web+nostr protocol handler", error)' src/index.tsx` outputs `1` — the caught error is passed to the logger, which is what clears silent-recovery
    - `grep -c 'logger("Rendering app")' src/index.tsx` outputs `1` — the pre-existing direct logger call is untouched
    - This command still outputs `4`, proving backlog 999.8's findings were not swept: `pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[] | select(.filePath=="src/index.tsx") | select(.rule=="ai-slop/trivial-comment")] | length'`
    - `pnpm build` completes successfully
  </acceptance_criteria>
  <done>The hidden-fallback finding is suppressed by a rule-scoped directive carrying a why-not-fixed reason; index.tsx logs the protocol-handler failure with its cause and carries no swallowed-exception, silent-recovery or console-leftover finding; both files' out-of-scope findings are still present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native barcode module → installNativeScanner | Capacitor plugin events drive promise settlement |
| browser API → registerProtocolHandler | Optional `web+nostr` handler registration, may be refused |
| local SQLite → openConnection / deleteDatabase | Native database connection and deletion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Denial of Service | `native-scanner.ts` promise settlement after the refactor | medium | mitigate | A restructure that loses a terminal case would hang the installer promise forever. Acceptance criteria assert all three terminal cases still call `sub?.remove()` and that both rejection messages survive, and `pnpm build` gates the type change. Residual: the path is native-only and cannot be exercised without a Capacitor build (recorded in 03-VALIDATION.md "Manual-Only Verifications"). |
| T-03-16 | Tampering | `sqlite/index.ts` error propagation after removing the wrappers | low | mitigate | An `async` function already returns a rejected promise for an uncaught throw, so removing `catch (err) { return Promise.reject(err); }` is semantically identical. Acceptance criteria assert both resolve paths and the `return db;` path survive, and that the module-level web guard is intact. |
| T-03-17 | Information Disclosure | `logger.extend("Index")` log call | low | accept | Logs only a fixed message plus the caught Error from a browser API refusal. No user data, key or credential is involved, and the `debug` logger is silent in production unless the namespace is enabled. |
| T-03-18 | Repudiation | Suppressing `hidden-fallback` with an inline directive | low | mitigate | D-07 makes an ignore a last resort and requires the reason to justify why the code could not be fixed. The directive is rule-scoped (suppressing only `ai-slop/hidden-fallback`, so every other rule still applies to that line), an acceptance criterion asserts exactly one directive and one reason delimiter, and another asserts the neighbouring `exhaustive-deps` finding still fires — proving the suppression is narrow. |

No new attack surface: no new dependency, network call, parser or exported API. The protocol-handler
registration and the sqlite guard behave exactly as before. ASVS L1.
</threat_model>

<verification>
```bash
# Every bucket-B finding this plan owns, across all four files, must be gone.
pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]
  | select(.filePath | test("^src/(services/sqlite/index\\.ts|components/qr-code/native-scanner\\.ts|hooks/timeline/use-timeline-cache-key\\.ts|index\\.tsx)$"))
  | select(.rule=="ai-slop/redundant-try-catch" or .rule=="eslint/no-async-promise-executor" or .rule=="ai-slop/hidden-fallback" or .rule=="ai-slop/swallowed-exception" or .rule=="ai-slop/silent-recovery")]
  | length'
# expect: 0   (reads 6 before this plan — 2 redundant-try-catch, 1 async-executor, 1 hidden-fallback,
#              1 swallowed-exception, 1 silent-recovery)

# Out-of-scope findings in the same files must still be present (no drive-by sweeps).
pnpm exec aislop scan --json . 2>/dev/null | jq '[.diagnostics[]
  | select(.rule=="eslint/no-unreachable" or .rule=="ai-slop/trivial-comment" or .rule=="react-hooks/exhaustive-deps")
  | select(.filePath | test("^src/(services/sqlite/index\\.ts|hooks/timeline/use-timeline-cache-key\\.ts|index\\.tsx)$"))]
  | length'
# expect: 11   (6 no-unreachable in sqlite + 4 trivial-comment in index.tsx + 1 exhaustive-deps)

pnpm build
```

This plan runs in parallel with 03-02, 03-03 and 03-04, so do not assert on the whole-repo bucket-B
total here. Never assert on the scan's exit code — it exits 1 whenever any finding exists anywhere in
the repo, which is always true.

**Residual risk, explicitly accepted:** `native-scanner.ts` is native-only code requiring a Capacitor
build to exercise at all. `pnpm build` plus the structural acceptance criteria are the accepted
substitute; see 03-VALIDATION.md "Manual-Only Verifications".
</verification>

<success_criteria>
- All four warning-severity strays cleared (2 × `redundant-try-catch`, `no-async-promise-executor`, `hidden-fallback`)
- `src/index.tsx` contributes 1 of the 31 error-severity findings and now contributes zero, with its `silent-recovery`, unused binding and `console-leftover` cleared in the same edit
- `pnpm build` passes
- Phase 4's territory in `sqlite/index.ts` (lines 1-16 and the `dbName` binding) is provably untouched
- Backlog 999.5's and 999.8's findings in the touched files are provably still present
</success_criteria>

## Artifacts this phase produces

This plan creates **no new files and no new exported symbols**. New identifiers:

| Symbol | File | Kind |
|--------|------|------|
| `log` (`logger.extend("Index")`) | src/index.tsx | module-private const |
| `sub` (mutable listener handle inside the promise executor) | src/components/qr-code/native-scanner.ts | function-local let |

Symbols removed: none exported. Two `try`/`catch` wrappers deleted in `src/services/sqlite/index.ts`
without changing either function's signature (`openConnection` and `deleteDatabase` keep their exact
parameter lists and return types).

<output>
Create `.planning/phases/03-audit-swallowed-exceptions-and-silent-failure-paths/03-05-SUMMARY.md` when done.
Record the type annotation chosen for the hoisted listener handle in `native-scanner.ts`, since research
flagged more than one valid shape (Assumption A2).
</output>
