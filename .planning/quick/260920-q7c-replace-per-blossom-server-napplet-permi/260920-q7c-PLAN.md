---
phase: quick-260920-q7c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/global/napplet-shell-provider.tsx
  - .changeset/quiet-napplets-fetch.md
autonomous: true
requirements: [QUICK-260920-NAPPLET-RESOURCE]

estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - If the user approved a napplet's consent prompt ("Allow once" or "Always allow") and `resource:fetch` was on the list, that napplet can fetch any http(s) URL through resource.bytes / resource.bytesMany, from any server, with no further prompt.
    - No per-server "network access" prompt exists anywhere. The shell provider renders exactly two modals, the capability consent modal and the intent-choice modal.
    - A napplet without an approved `resource:fetch` can still fetch from the active account's own kind-10063 Blossom servers, exactly as today, and is denied with `blocked-by-policy` for every other origin - with no prompt offered.
    - A URL whose scheme is not http or https is denied with `blocked-by-policy`.
    - The following are gone from the code - the per-origin always-allow localStorage list, session grant tracking, the grant queue, and the "Allow network access?" modal. The kind-10063 origin allowlist (`getBlossomOrigins`, fed by `BlossomServersQuery`) stays, and Blossom upload still uses the user's server list, unchanged.
  artifacts:
    - path: src/providers/global/napplet-shell-provider.tsx
      provides: "resource service allowing a fetch when the user approved resource:fetch or the origin is one of the account's Blossom servers; per-origin prompt, storage and session state removed"
      contains: "approvedCapabilities"
    - path: .changeset/quiet-napplets-fetch.md
      provides: "patch changeset describing the napplet resource permission change"
      contains: "\"nostrudel\": patch"
  key_links:
    - from: src/components/napplets/napplet-frame.tsx (requestConsent(active, identity, getNappletRequiredCapabilities(active)))
      to: grantCapabilities in src/providers/global/napplet-shell-provider.tsx
      via: "requestConsent's auto-grant path and respond(true) both call grantCapabilities, which records the approved capability set under identityKey(identity)"
      pattern: "approvedCapabilities.set"
    - from: registerFrame(windowId, win, { pubkey, dTag, aggregateHash, title }) in napplet-frame.tsx
      to: fetchOne in createResourceService
      via: "windowIdentities.get(windowId) yields the same pubkey/dTag/aggregateHash triple as the consent identity, so identityKey lookups into approvedCapabilities match"
      pattern: "windowIdentities.get"
    - from: BlossomServersQuery -> blossomOrigins -> blossomOriginsRef in NappletShellProvider
      to: createResourceService options.getBlossomOrigins
      via: "the account's kind-10063 server origins remain an implicit allowance, so an unapproved napplet keeps today's access to the user's own servers"
      pattern: "getBlossomOrigins"
    - from: src/helpers/nostr/napplets.ts REQUIRED_CAPABILITY_MAP (resource -> resource:fetch)
      to: consent modal capability list
      via: "a manifest `requires resource` tag is what puts resource:fetch in front of the user for approval"
      pattern: "resource: \\[\"resource:fetch\"\\]"
---

<objective>
Replace the per-origin (per-Blossom-server) network prompt for napplets with one gate. If the user approved the NAP-RESOURCE capability (`resource:fetch`) in the normal napplet consent modal, the napplet may fetch any http(s) URL. Fetches from the account's own Blossom servers keep working without that approval, as they do today.

Purpose: The user asked for this in these words: "if the user approves just the nap-resource permission then the napplet can download resources from blossom servers". Today `createResourceService` in `src/providers/global/napplet-shell-provider.tsx` allows an origin only in three cases. First, it is one of the active account's Blossom servers (kind 10063 list). Second, it is in a localStorage "always allow" list keyed per napplet identity plus origin. Third, the user answers an "Allow network access?" modal shown for each new origin. The approval in the consent modal never enters into it, so a napplet fetching from servers outside the user's list gets one prompt per server. This plan deletes cases two and three, keeps case one, and makes the consent-modal approval the thing that unlocks everything else.

Output: A rewritten allowance check in the shell provider, with the per-origin type, storage helpers, provider state, callbacks and modal deleted. Plus a patch changeset.
</objective>

<execution_context>
@/home/user/.claude/gsd-core/workflows/execute-plan.md
@/home/user/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/home/user/Projects/noStrudel/AGENTS.md
@/home/user/Projects/noStrudel/.claude/CLAUDE.md
@/home/user/Projects/noStrudel/src/providers/global/napplet-shell-provider.tsx

<interfaces>
<!-- Facts gathered while planning. The executor does not need to re-derive them. -->

Why the runtime ACL cannot be the gate (verified in node_modules/@kehto/runtime/dist/index.js and @kehto/acl/dist/index.js):
- `createRuntime` builds its ACL with `createAclState(hooks.aclPersistence, "permissive", ...)`.
- For an identity with no entry, `check()` returns `state.defaultPolicy === "permissive"`, i.e. true.
- `aclState.grant()` calls `ensureRuntimeDefaultEntry` first. Under permissive policy that seeds the entry with every capability (`RUNTIME_CAP_ALL`) before adding the requested one.
- The runtime gates `resource.bytes` / `resource.bytesMany` / `resource.info` / `resource.cancel` on sender capability `resource:fetch` (`resourceMap` in @kehto/acl). Given the two points above, that check always passes. `bridge.runtime.aclState` therefore cannot tell whether the user approved `resource:fetch`. The shell has to keep its own record of the approved list.

Consent flow (src/components/napplets/napplet-frame.tsx lines 97-116, 167-172):
- `const capabilities = getNappletRequiredCapabilities(active);` builds the list from manifest `requires` tags. `requires resource` maps to `["resource:fetch"]`.
- `requestConsent(active, { pubkey: active.pubkey, dTag: resolved.dTag, aggregateHash: resolved.aggregateHash }, capabilities)` runs before the frame mounts. If it resolves false, the frame never mounts.
- `registerFrame(windowId, node.contentWindow, { pubkey: active.pubkey, dTag: napplet.dTag, aggregateHash: napplet.aggregateHash, title })` uses the same triple, so `identityKey()` keys agree.

Existing shell-provider pieces to reuse (src/providers/global/napplet-shell-provider.tsx):
- `type NappletIdentity = { pubkey; dTag; aggregateHash; title? }` and `identityKey(identity)` returns `${pubkey}:${dTag}:${aggregateHash}`.
- `grantCapabilities(bridge, identity, capabilities)` is called from `requestConsent`'s auto path (always-allowed, or empty capability list) and from `respond(true, ...)`.
- `windowIdentities` is a module-level map from windowId to identity, filled by `registerFrame` and cleared by `unregisterFrame`.
- `sendResourceError(send, requestId, "denied", message)` maps to NAP error `blocked-by-policy`. The other two codes it special-cases are `invalid-url` and everything else (network-error), so a policy refusal must use `"denied"`.
- The kind-10063 chain stays exactly as it is: `BlossomServersQuery` -> `blossomServers` -> `blossomServerUrls` -> `blossomOrigins` (unique origins) -> `blossomOriginsRef` -> the adapter's `resource.getBlossomOrigins`.
- `Capability` type is already imported from `@kehto/shell`.

aislop baseline for src/providers/global/napplet-shell-provider.tsx (measured while planning): 0 errors, 20 warnings.
</interfaces>
</context>

<!-- planner-discipline-allow: RESOURCE_ALWAYS_ALLOW -->
<!-- planner-discipline-allow: ResourceConsentRequest -->
<!-- planner-discipline-allow: ResourceIdentity -->
<!-- planner-discipline-allow: requestResourceGrant -->
<!-- planner-discipline-allow: resourceGrantKey -->
<!-- planner-discipline-allow: AlwaysAllowedResourceOrigin -->
<!-- planner-discipline-allow: respondResource -->
<!-- planner-discipline-allow: resourceConsent -->
<!-- planner-discipline-allow: sessionResourceGrants -->
<!-- planner-discipline-allow: resourceGrantQueue -->
<!-- planner-discipline-allow: Allow network access -->

<tasks>

<task type="tracer">
  <name>Task 1: Allow napplet fetches on approved resource:fetch or a user Blossom origin, and delete the per-origin prompt path</name>
  <files>src/providers/global/napplet-shell-provider.tsx</files>
  <action>
All edits are in `src/providers/global/napplet-shell-provider.tsx`. The path runs from consent, to the recorded approval, to `fetchOne`, to the network. The old per-origin prompt chain is the same code path being replaced, so delete it in this task rather than leaving it unreferenced.

New gate:
1. Next to `windowIdentities`, add a module-level `approvedCapabilities` map of type `Map<string, Set<Capability>>`, keyed by `identityKey(identity)`. Give it a one-line comment explaining why it exists: the kehto runtime ACL is permissive, so identities with no entry pass every check and the first grant seeds all capabilities, which means the runtime cannot report what the user actually approved.
2. In `grantCapabilities`, after the `aclState.grant` loop, add `approvedCapabilities.set(identityKey(identity), new Set(capabilities))`. Use replace, not merge. Every frame load calls `requestConsent` again with the manifest's full list, so the latest approval is the one that counts.
3. In the provider's `respond` callback, when `allow` is false, delete the entry for `identityKey(consent.identity)`. A same-session denial must not leave an earlier approval active.
4. Add a helper `hasApprovedCapability(identity: NappletIdentity, capability: Capability)` returning `approvedCapabilities.get(identityKey(identity))?.has(capability) ?? false`.
5. In `createResourceService`, keep the options object but reduce it to `{ getBlossomOrigins: () => string[] }`. Delete the grant-requesting option and replace the async origin check with a synchronous `isAllowed(identity: NappletIdentity, origin: string)` that returns true when either the identity has an approved `resource:fetch` or `options.getBlossomOrigins().includes(origin)`. Keeping the kind-10063 branch is deliberate: a napplet with no approved capability must retain today's access to the account's own servers.
6. In `fetchOne`, keep the invalid-URL step and the identity lookup as they are. Then add two checks, each followed by `return`:
   - If `parsed.protocol` is neither `"https:"` nor `"http:"`, send a `"denied"` error whose message names the rejected scheme. Use the `"denied"` code so the napplet-facing error stays `blocked-by-policy`.
   - If `isAllowed(identity, parsed.origin)` is false, send a `"denied"` error whose message says the origin is not allowed because `resource:fetch` was not approved for this napplet.

   Drop the `await` that the old asynchronous check needed. Do not change anything else in `fetchOne`: the size caps, abort tracking, result shape and bytesMany concurrency stay as they are. Change the service descriptor `description` to `NAP-RESOURCE fetch gated on approved resource:fetch or a user Blossom origin`.
7. In `createAdapter`, narrow the `resource` parameter to `{ getBlossomOrigins: () => string[] }` and keep passing it to `createResourceService(resource)`. In `NappletShellProvider`, the adapter call keeps `{ getBlossomOrigins: () => blossomOriginsRef.current }` as that argument, and its `useMemo` deps become `[toast, getIntentNavigator, chooseIntentHandler, upload.enabled]`.

Deletions. The user asked for the per-server prompt plumbing to be removed, not left as dead code:
8. Module scope: delete the `ResourceConsentRequest` type, the `RESOURCE_ALWAYS_ALLOW_STORAGE_KEY` constant, and the functions `resourceGrantKey`, `getAlwaysAllowedResourceOrigins`, `addAlwaysAllowedResourceOrigin` and `isAlwaysAllowedResourceOrigin`. Delete the `ResourceIdentity` alias too: it is a `Pick` of every field of `NappletIdentity`, so it narrows nothing and only existed for the grant plumbing. Retype `windowIdentities` as `Map<string, NappletIdentity>` and use `NappletIdentity` in the resource service signatures.
9. `NappletShellProvider`: delete the `resourceConsent` state, `sessionResourceGrantsRef`, `resourceGrantQueueRef`, the `requestResourceGrant` callback, the `respondResource` callback, and the whole second `<Modal>` (header "Allow network access?").
10. Keep, untouched: `blossomServers`, `blossomServerUrls`, `blossomOrigins`, `blossomOriginsRef` and its render-time `.current` assignment, `upload`, `uploadRef`, `createBlossomUploadService`, and the `unique` import (`getWriteRelays` uses it).

Constraints:
- Do not write tombstone comments that name removed helpers.
- Do not add migration code to clear the old localStorage key. Napplets are unreleased (CHANGELOG.md has no napplet entry) and nothing reads the key any more.
- Do not add a Blossom-URL or sha256 shape check. The approved capability covers any http(s) URL; that is the user's decision.
- Use relative imports and the existing code style (AGENTS.md).
- Fix any new aislop hook finding this edit introduces. Do not sweep the file's pre-existing warnings (.claude/CLAUDE.md override, D-16).
  </action>
  <verify>
    <automated>cd /home/user/Projects/noStrudel && F=src/providers/global/napplet-shell-provider.tsx && pnpm exec tsc --project tsconfig.json && test "$(grep -vE '^\s*(//|\*)' "$F" | grep -cE 'RESOURCE_ALWAYS_ALLOW|ResourceConsentRequest|ResourceIdentity|requestResourceGrant|resourceGrantKey|AlwaysAllowedResourceOrigin|respondResource|resourceConsent|sessionResourceGrants|resourceGrantQueue|Allow network access')" = 0 && test "$(grep -c 'approvedCapabilities' "$F")" -ge 3 && test "$(grep -c 'getBlossomOrigins' "$F")" -ge 3 && test "$(grep -c 'createResourceService(resource)' "$F")" = 1 && test "$(grep -c '"resource:fetch"' "$F")" -ge 1 && test "$(grep -c '"https:"' "$F")" -ge 1 && test "$(grep -c '<Modal isOpen' "$F")" = 2 && grep -q 'Grant napplet access?' "$F" && grep -q 'createBlossomUploadService(getUpload)' "$F" && grep -q 'blossomOriginsRef' "$F" && echo OK</automated>
  </verify>
  <done>
- `tsc` passes.
- None of the per-origin prompt identifiers, nor the network-access modal, remain outside comments.
- `fetchOne` denies with `blocked-by-policy` unless the scheme is http(s) and either the window's identity has a user-approved `resource:fetch` or the origin is one of the account's Blossom servers.
- The consent and intent-choice modals are both still rendered.
- The kind-10063 origin chain and the Blossom upload wiring are unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the patch changeset and run the touched-file lint and build gates</name>
  <files>.changeset/quiet-napplets-fetch.md</files>
  <action>
1. Create `.changeset/quiet-napplets-fetch.md` in the same format as `.changeset/clever-napplets-link.md`. The frontmatter is `"nostrudel": patch`. The body is one sentence: `Approving a napplet's NAP-RESOURCE permission now covers every server, replacing the per-server network prompt`.
2. Run the aislop scan scoped to `src/providers/global/napplet-shell-provider.tsx`.
   - It must report 0 error-severity findings.
   - It must report no more than the planning baseline of 20 warnings. The deletions shorten the provider function, so the count should drop.
   - If Task 1 introduced a finding, fix it in the provider file and re-run. Leave pre-existing warnings alone.
3. Run `pnpm build`. This is the project's configured test/build command in `.planning/config.json`, and the repo has no unit-test runner.
  </action>
  <verify>
    <automated>cd /home/user/Projects/noStrudel && grep -q '"nostrudel": patch' .changeset/quiet-napplets-fetch.md && pnpm exec aislop scan --json --include src/providers/global/napplet-shell-provider.tsx . 2>/dev/null | jq -e '[.diagnostics[] | select(.filePath=="src/providers/global/napplet-shell-provider.tsx")] | ((map(select(.severity=="error")) | length) == 0) and (length <= 20)' && pnpm build</automated>
  </verify>
  <done>
- The changeset exists with a patch bump.
- The provider file has 0 aislop errors and at most 20 warnings.
- `pnpm build` (tsc plus vite build) succeeds.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| napplet iframe -> shell (postMessage) | Untrusted napplet code (`sandbox="allow-scripts"`) sends resource.bytes / resource.bytesMany requests with URLs and fetch init it chooses |
| shell -> remote HTTP server | The shell runs `fetch()` from noStrudel's origin on the napplet's behalf and hands the bytes back |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-q7c-01 | Elevation of privilege | `isAllowed` / `fetchOne` in createResourceService | high | mitigate | The kehto ACL is permissive, so its `resource:fetch` gate always passes. `isAllowed` therefore checks `hasApprovedCapability(identity, "resource:fetch")` against the `approvedCapabilities` record that `grantCapabilities` writes only after user consent (the modal, or a stored "Always allow"). A same-session deny clears the record. |
| T-q7c-02 | Information disclosure | shell-side `fetch()` of napplet-chosen URLs after approval | medium | accept | An approved napplet can reach any http(s) origin, including private or LAN addresses, from noStrudel's origin. This is the user's explicit decision: one approval covers every server. Residual limits are the consent modal naming `resource:fetch` before approval, and the browser's CORS policy, which still decides whether a cross-origin response body can be read back. |
| T-q7c-03 | Information disclosure | unapproved napplet reading blobs from the account's own Blossom servers | low | accept | The kind-10063 origin allowance is retained deliberately so today's behaviour is preserved. A napplet must still know a blob's sha256 to fetch it. |
| T-q7c-04 | Spoofing | non-http schemes handed to `fetch()` (`data:`, `file:`, custom) | low | mitigate | `fetchOne` rejects any URL whose protocol is not `https:` or `http:` with `blocked-by-policy`, before the allowance check runs. |
| T-q7c-05 | Denial of service | large or many fetches | low | mitigate | `MAX_RESOURCE_BYTES` (25 MiB), `MAX_RESOURCE_URLS` (16) and `MAX_CONCURRENT_RESOURCE_FETCHES` (4) stay in `fetchOne` and `resource.bytesMany` unchanged. Per-window abort on destroy also stays. |
</threat_model>

<verification>
- Task 1's gate: `tsc` passes, the removed identifiers are absent, the new gate identifiers are present, exactly two modals are rendered, and both the kind-10063 chain and the upload service are still wired.
- Task 2's gate: the changeset exists, the provider file has 0 aislop errors and at most 20 warnings, and `pnpm build` succeeds.
- Optional manual check (not blocking; `pnpm dev` has been unreliable on this machine per STATE.md):
  - Open a napplet whose manifest has `requires resource` and approve the consent prompt. It should fetch from a server that is not on your kind-10063 list with no second prompt.
  - Open a napplet without `requires resource`. It should still fetch from your own Blossom servers, and fail with `blocked-by-policy` for any other origin.
</verification>

<success_criteria>
- The permission governing napplet fetches beyond the account's own Blossom servers is the `resource:fetch` capability the user approves in the existing consent modal.
- No per-server prompt or per-origin localStorage grant remains in the code.
- Non-http(s) URLs, and other origins for napplets without approval, are denied with `blocked-by-policy`.
- Fetches from the account's kind-10063 Blossom servers, Blossom upload, and all other shell services are unchanged.
- `pnpm build` passes, and the touched file has no new aislop errors.
</success_criteria>

<output>
Create `/home/user/Projects/noStrudel/.planning/quick/260920-q7c-replace-per-blossom-server-napplet-permi/260920-q7c-SUMMARY.md` when done.
</output>
